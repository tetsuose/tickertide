{
  "version": 1,
  "updated_at": "2026-06-13T14:21:13.887702+00:00",
  "policy": {
    "stage_values": [
      "scaffolded",
      "baseline",
      "planned",
      "implemented",
      "verified",
      "done",
      "deprecated"
    ],
    "replan_rule": "When repo structure changes, run `make writeback-apply WRITE=1` before continuing."
  },
  "entries": [
    {
      "notes": "",
      "path": ".env.example",
      "kind": "config",
      "module": ".",
      "stage": "implemented",
      "contract": {
        "purpose": "secrets 模板（names-only）：STOOQ_APIKEY 等",
        "invariants": "只记名不记值；真实 .env 走 gitignore；STOOQ_APIKEY 仅 --provider stooq 需要",
        "verification": "git check-ignore 确认真实 .env 被忽略；本模板入库且不含值"
      }
    },
    {
      "notes": "",
      "path": ".gitattributes",
      "kind": "config",
      "module": ".",
      "stage": "implemented",
      "contract": {
        "purpose": "统一跨平台换行为 LF 并标记二进制文件，避免大小写不敏感 macOS 与大小写敏感 Linux 间的伪 diff",
        "invariants": "文本一律 eol=lf；*.parquet/*.duckdb 标记为 binary",
        "verification": "git check-attr 对源文件返回 text；Linux CI checkout 后无 EOL 噪音 diff"
      }
    },
    {
      "notes": "",
      "path": ".github/workflows/deploy-web.yml",
      "kind": "config",
      "module": ".github",
      "stage": "implemented",
      "contract": {
        "purpose": "快速纯前端部署通道：push 到 main 且只动 web/**(或本文件、或 workflow_dispatch)时，不重跑 21–29min 数据管线，而是从最近一次成功 nightly run 下载 tickertide-site artifact、把其中 public/data/ 放回 web/public/data、只跑 npm ci+build，再用与 nightly 完全相同的 wrangler Pages 步骤发布到生产 tickertide.pages.dev",
        "invariants": "数据只来自 nightly artifact(本通道永不 ingest/compute——重算仍是 nightly 的职责)；artifact 布局依赖 nightly upload 的最小公共祖先 web/ 被剥离(根下为 dist/+public/data/)，nightly 改 upload paths 时本文件须同步；nightly retention=7 天——近 10 次成功 run 都无未过期 artifact 时 fail-early 并提示先跑 nightly，绝不部署无数据站点；deploy gate 与 nightly 一致(只 echo 布尔不打印 token、secret 未设自动 skip)；token 仅存 GitHub Actions secret(CLOUDFLARE_API_TOKEN/_ACCOUNT_ID)；permissions 含 actions:read(列 run+下载 artifact)；concurrency 同组取消旧 run",
        "verification": "workflow_dispatch 手测：有近期 nightly artifact 时全流程绿、tickertide.pages.dev 数据 as_of 与该 nightly 一致且前端为新代码；artifact 全过期场景 locate 步骤红且错误信息含 run nightly first；run log 不含 token 值"
      }
    },
    {
      "notes": "",
      "path": ".github/workflows/gate.yml",
      "kind": "config",
      "module": ".github",
      "stage": "implemented",
      "contract": {
        "purpose": "advisory CI：每次 push/PR 在 Linux runner 跑 devtopology gate(make verify + make health) 与离线合成管道验证(fixture→compute→valuation→export→check)",
        "invariants": "不作为 required check 阻塞合并（free private 无 branch protection）；gates job 跑 stdlib-only 引擎 gate、offline-pipeline job 装 requirements.txt 用合成 fixture 给无网络的数据管道(compute/export)以 CI 覆盖、make export 全套(含 valuation.parquet+stock bundle)、check-land(M4.5)、valuation-c9(M5.5 AC-M5 C9)；其价值是 PR 上留客观健康记录并验证小写路径；所用 actions 维持受 GitHub 支持的 Node24 runtime、不依赖已弃用的 node20 runtime",
        "verification": "push 后 GitHub Actions 出现 verify/health 日志与 offline-pipeline 的 CHECK_OK(含 check-land)；gate 输出可见；board.json 作为 artifact 上传；workflow 运行无 Node20 runtime 弃用告警"
      }
    },
    {
      "notes": "",
      "path": ".github/workflows/nightly.yml",
      "kind": "config",
      "module": ".github",
      "stage": "implemented",
      "contract": {
        "purpose": "夜间 EOD 数据管道 + 部署(D.2+D.3)：cron(美东盘后) + workflow_dispatch 在 GitHub runner 跑真实 pipeline(ingest+sector_etf→fundamentals→themes→compute→check) + export(board/ocean/rotation/theme) + C9 + web-build → 上传 site+data artifact → 直传 Cloudflare Pages 生产公开上线",
        "invariants": "真实数据源(Nasdaq/yfinance/EDGAR 公开端点)；deploy token 仅存 GitHub Actions secret(CLOUDFLARE_API_TOKEN/_ACCOUNT_ID)、绝不入库/PR/日志、gate 步骤只 echo 布尔不打印值；deploy 在 pipeline+C9+web-build 全绿后才跑(失败步骤先中断 job→陈旧/失败不发布)；CLOUDFLARE_API_TOKEN secret 未设时 deploy 步骤自动跳过(脚手架可安全先合并)；首次 go-live+启用 auto-deploy 是 hard stop(需用户点头)；只装 requirements.txt 产品 deps、engine/index.py 仍 stdlib-only；concurrency 防重叠、timeout 50min；theme 链路随 make pipeline/export 产 rotation.theme.json 并过 theme-c9(M4.6)；M5 export 产 valuation.parquet+stock bundle 并过 valuation-c9(C9)、web build 设 VITE_DUCKDB_R2_BASE(R2 wasm)；所用 actions 维持 Node24 runtime、不依赖已弃用的 node20；web build 设 VITE_DUCKDB_R2_BASE=vars.DUCKDB_R2_BASE(repo 变量、公开 URL 非 secret;指 R2 托管的 duckdb-wasm，未设则 client fallback jsDelivr，build 安全)",
        "verification": "workflow_dispatch 手测(gh workflow run -f limit=小)跑通、artifact 含最新 as_of 的 dist+data；rotation-c9/theme-c9/ocean-c9 在 CI 真实数据上 PASS；设 secret 后 deploy 步骤执行、未设时 skip 不报错；run log 不含 token 值；engine 不装 deps、无 Node20 弃用告警"
      }
    },
    {
      "notes": "",
      "path": ".gitignore",
      "kind": "config",
      "module": ".",
      "stage": "implemented",
      "contract": {
        "purpose": "排除生成物、产品数据产物与凭证文件，使版本库只跟踪源与规格",
        "invariants": "必须含 .index/、__pycache__/、data/、*.duckdb、*.parquet、secrets/、*.env、.wrangler/、.dev.vars(Cloudflare 本地 state/secret)；排除 themes/candidates/(M4.5 LLM 中间产物,可重跑)、保留 themes/approved/(人审 SoT)；保留 *.env.example 与 docs/runtime 的 .example 模板",
        "verification": "git status 不出现生成物/数据/密钥/wrangler 本地态；clone 后无被忽略文件被跟踪"
      }
    },
    {
      "notes": "",
      "path": "AGENTS.md",
      "kind": "doc",
      "module": ".",
      "stage": "implemented",
      "contract": {
        "purpose": "跨 agent 入口（agents.md 开放标准），供 Codex/Cursor/Copilot/Gemini",
        "invariants": "薄入口不承载滚动状态；合同格式示例与账本 schema 一致；命令须为真 make target",
        "verification": "所列命令均为有效 make target；合同示例字段与 File-Contracts.json 匹配"
      }
    },
    {
      "notes": "",
      "path": "CLAUDE.md",
      "kind": "doc",
      "module": ".",
      "stage": "implemented",
      "contract": {
        "purpose": "Claude Code 项目入口：脊柱约束 + 导航 + 工作流规则 + 关键命令",
        "invariants": "薄入口不承载滚动状态；命令须为有效 make target；约束与 PRD §7/§16 同步",
        "verification": "列出命令均为真 target；已定死清单与 PRD §16 一致"
      }
    },
    {
      "notes": "",
      "path": "Makefile",
      "kind": "script",
      "module": ".",
      "stage": "implemented",
      "contract": {
        "purpose": "项目命令入口：工作流路由/结构门禁/合同 + 数据管道(ingest→compute→export) + web 构建/测试 + 离线 fixture",
        "invariants": "工作流引擎 engine/index.py stdlib-only、产品代码走 venv、web/ 走 npm；export 产 board.json + ocean.json→web/public/data；ocean-c9=C9 跨 surface 校验(ocean 点位==board/Stock 数字 AC-M2)；web-build=export+vite 产 web/dist；web-test=vitest(C9 parity + AC-M1/M2)；fixture/fixture-pipeline 离线喂真实引擎；ingest 含 sector_etf(bucket_bars)、compute 含 rotation(bucket_rrg)+theme_index+theme rotation(M4)、export 含 rotation.json+rotation.theme.json+valuation.parquet(M5.1 screener 分片)+stock/<T>.json per-name bundle(M5.3 价格/营收/PS 时序)、rotation-c9/theme-c9 跑 check_rotation(AC-M3/AC-M4)、valuation-c9 跑 check_valuation(AC-M5 C9 valuation/stock↔board)；themes=seed 幂等重播 + land(themes/approved/*.json 人审 SoT，M4.5)、theme-extract=operator 抽取(EDGAR→claude CLI，非 CI)、check-land=land+PIT 优先级离线验收(M4.5)；pipeline=ingest→fundamentals→themes→compute→check(含 check-theme AC-M4)；export 末尾跑 manifest.py(新鲜度 manifest，D.4)",
        "verification": "make help 列全目标；make pipeline/fixture-pipeline/web-build/web-test/ocean-c9/rotation-c9/theme-c9/check-theme/check-land 端到端可跑；make verify GATE_PASS"
      }
    },
    {
      "notes": "",
      "path": "README.md",
      "kind": "doc",
      "module": ".",
      "stage": "implemented",
      "contract": {
        "purpose": "人类可读的项目介绍、架构图、仓库结构与工作流速查",
        "invariants": "文档链接有效；架构与里程碑与 PRD 同步；命令为真",
        "verification": "README 内链接可达；命令为有效 make target"
      }
    },
    {
      "notes": "negative-then-refined 结果:钉出 Discovery 应为『持续点火』榜而非瞬时;下一步落地 compute/ignition.py + persistence",
      "path": "analysis/precision_ignition.py",
      "kind": "script",
      "module": "analysis",
      "stage": "baseline",
      "contract": {
        "purpose": "ignition 精度回看(precision pass):在中性的 Nasdaq habitat 池($0.3B-$100B 随机 800 只)上量化 ignition top-decile 点亮后的延续 vs 假阳性。核心发现=瞬时点火 lift≈0(证伪『Discovery 直接改 ignition 排序』),唯『持续点火』(连续在 top decile)把 60-120d 中位 LIFT 转正;一次性分析脚本,非 nightly 管线",
        "invariants": "ignition 复用 analysis/verify_ignition.py 的 ignition_metrics 同一套数学;universe 来自 ingest/nasdaq.fetch_universe 真实美股(非手挑、降幸存者挑选偏差);核心度量是『event 减 base 的 LIFT』而非绝对收益(抗 2023-26 牛市底噪);只读 yfinance,产出仅落 gitignore 的 data/;幸存者偏差(死票缺失)在输出显式标注、不掩饰",
        "verification": "跑 .venv/bin/python analysis/precision_ignition.py 输出 4 变体(top10/5/1% 进入 + top10%持续5d)× 3 窗口的 median LIFT vs base 表 + 4 target 的 recall;唯持续过滤变体 LIFT 显著为正"
      }
    },
    {
      "notes": "in-sample 时机验证,非 precision/胜率证明;下一步 precision pass 会在此基础上演进",
      "path": "analysis/verify_ignition.py",
      "kind": "script",
      "module": "analysis",
      "stage": "baseline",
      "contract": {
        "purpose": "ignition vs composite 早期点亮时机的历史回看验证:用真实 yfinance 数据量化短窗口 ignition 引擎是否比现有长窗口 composite 更早把已知大牛股(ARM/MRVL/AAOI/SNDK)送进 universe 前 10%(双引擎方向的实证,PRD §1 脊柱);一次性分析脚本,非 nightly 管线",
        "invariants": "composite 复用 compute/signals.py 产品口径(横截面 rs_pct/composite 镜像 compute/run.py),绝不另起一套数学;ignition 仅由短窗口/拐点/突破分量构成;两引擎用同一『横截面 top-decile 点亮』口径以保证时机对比公平;只读 yfinance 价格,产出仅落 gitignore 的 data/,不写产品 DuckDB 或 web 数据",
        "verification": "跑 .venv/bin/python analysis/verify_ignition.py 输出每只目标的 t0 与 ignition/composite 点亮周数+点亮时涨幅对照表;4 只目标上 ignition 点亮均不晚于 composite"
      }
    },
    {
      "notes": "",
      "path": "compute/README.md",
      "kind": "doc",
      "module": "compute",
      "stage": "planned",
      "contract": {
        "purpose": "DuckDB 计算引擎模块规划：derived_daily + composite + valuation + RS-Ratio",
        "invariants": "所有 surface 共用单一 per-stock 数据（C9）；买 robustness 不买 alpha 不回测优化权重",
        "verification": "计算内容与 PRD §4/§10 一致；输出表对齐 §12"
      }
    },
    {
      "notes": "",
      "path": "compute/check.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "AC-M0+AC-M7 验收抽查：pipeline 跑完后 sanity-check DuckDB（PRD §14/§10.8）",
        "invariants": "只读 DB 不改状态；CI 友好（任一硬检查失败 exit 非0）；检查 universe present / daily_bars 无 NULL close / composite 5 分量∈[0,1] / valuation 无 lookahead 与无负 P/E / ignition 已生成且 ignition∈[0,100]、ign_pct∈[0,100]、ign_persist_days≥0(C9 同源 derived_daily)",
        "verification": "make check 对 M0 DB 输出 CHECK_OK 且每项 PASS（含 ignition 三项）；空/坏 DB 输出 CHECK_FAIL 并 exit 1"
      }
    },
    {
      "path": "compute/check_ac_m7.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "M7.5 AC-M7 聚合验收（PRD §14、§10.8）：把 AC-M7 五条收成单一带标签门禁，使整个里程碑可一键复验而非靠口述「哪个 target 覆盖哪一条」——补足此前散落在 make check(compute 列) / ignition-c9(C9) / web-test(渲染序)的拼图，提供 1:1 映射 AC-M7 五条的单命令复验",
        "invariants": "读已导出的 board.json + per-name stock bundles(浏览器同款产物)，逐条断言 AC-M7：① derived_daily 含 ignition 5 原始分量(accel/expand/vsurge/breakout/rsturn)+ign_pct+ign_persist_days 且覆盖全票、在范围内；② Discovery 持续点火序(candidate→persist desc→pct desc，与 web/src/views/Discovery.tsx ignKey 逐位一致)在该快照上确实 ≠ composite 纯排序(证明非 composite 排序)且按文档键单调非增；③ 每票 ignition.evidence 含点火证据六键(breakout_day/days_since_breakout/vol_mult/step_rate_ratio/reclaimed_ma50/ma50)；④ ignition⇄composite 同源(C9)：同一 per-stock 行成对出现(composite 有⇔ignition 有)+ ignition_recon_max_drift≈0 + 抽样 stock bundle 的 ignition 块与 board 逐字段相同；⑤ 持续点火榜非空(≥1 candidate)且每个 candidate 重算 gate(ign_pct≥90 AND persist≥persist_min)成立。复用 export/check_ignition.check 的同源/candidate/vsurge/stock↔board 逻辑(DRY，C9 逻辑单一真源)，仅新增 AC-M7.2 的 composite-vs-ignition 序分歧断言与逐条框架；任一条 FAIL 非零退出可门禁；names/counts only 无 secrets",
        "verification": "make ac-m7（须先 make export 产 board.json+stock bundles）→ 五条全 PASS + GATE_PASS；make ac-m7-e2e 从裸离线 DB 一命令端到端(fixture-pipeline → export → 五条)；构造不符(翻 candidate / 篡改 vol_mult / 制造 ignition==composite 同序)可触发 GATE_FAIL"
      },
      "notes": "M7.5 收口：AC-M7 五条单命令复验。复用 export/check_ignition.check 不重写 C9 逻辑；fixture 30 票实测 candidates=2(persist≥5)、ign_top≠composite_top、stock_xcheck=8 全过"
    },
    {
      "notes": "",
      "path": "compute/check_theme.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "AC-M4 验收抽查（M4.6，PRD §14）：pipeline 跑完后校验 theme 链路三性质——membership 形状(≥4 theme、exposure∈[0,1]、approved_by 必填 C6)、C3 晚到重述不回溯改写 index 历史、C4 权重 exposure-based 且 cap-bound(非市值加权)，外加 bucket_bars/bucket_rrg theme 序列落地的接线检查",
        "invariants": "三态结果 PASS/FAIL/SKIP：membership 形状(C6)、index-landed、C4 是硬 FAIL；RS-Ratio-landed 与 C3-no-retro 需要足够 index 历史(RS-Ratio 是周线算法需 ~半年交易日、C3 探针需 ≥10 index 日)，theme 历史不足时(真实场景 seed 单一近期 as_of → index 仅数日)这两项 SKIP(warn 非失败)而非 FAIL，使过短 theme 历史不阻断本就正确的 board/membership 的 export/deploy(根因修在 themes/seed.py as_of 回填)；对 DB 无净写入(C3 探针在 BEGIN/ROLLBACK 事务内插 exposure=0 重述、纯读 build_theme_index 对比前后段)；theme_membership 为空时 SKIP 且 exit 0；仅 FAIL 使 exit 非 0(SKIP 不算失败,CI 友好)",
        "verification": "make check-theme 对 fixture DB(长历史)输出 CHECK_OK 全 PASS、RS-Ratio/C3 不误 SKIP；构造 6 日短历史→RS-Ratio 与 C3 SKIP 且 CHECK_OK exit 0(不阻断 deploy)；清空 membership→SKIP exit 0；make check 串入"
      }
    },
    {
      "notes": "",
      "path": "compute/db.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "DuckDB 连接 + schema bootstrap + 幂等 upsert(含 bucket_bars/bucket_rrg) + compute/fundamentals/valuation/rotation 读写(M0–M3) + theme_membership point-in-time 读写(M4.1) + clear_bucket_bars 按 bucket_type 清(M4.2 theme index 重建)",
        "invariants": "single-writer offline batch；DB 在 data/ gitignored；upsert 幂等(INSERT OR REPLACE/ON CONFLICT)；read_* 返回 pandas；clear_derived/clear_valuation 支持全重算；upsert_bucket_bars 写 bucket(sector ETF / theme index)价、与 daily_bars 隔离(bucket 价不入 universe 横截面)、close 取 adj_close 同 upsert_spx；clear_bucket_bars 只删指定 bucket_type 行(theme index 重建前清 theme、sector ETF 行不动)；rotation 读写(bucket_names/read_bucket_bars 读 RS 源、upsert/read_bucket_rrg 管周线 RS-Ratio 序列、clear_bucket_rrg 按 bucket_type 限域清(M4.3：sector/theme rrg 共表共存，重建只清自己那型；None=全清向后兼容))同隔离原则；theme_membership_asof 为 PIT membership 唯一真源(M4.2 index/M4.4 chips/board 共用,语义只在此处定不在 caller 翻案)——per(ticker,theme) 取 as_of≤t 的最新行后再筛 exposure>0(C3 反回溯:晚 as_of 行不动早快照),可按 theme=/ticker= 收窄;upsert/clear/theme_keys 幂等管理",
        "verification": "connect 建表幂等；upsert 不产生重复行；read_* 返回 DataFrame；upsert_bucket_bars 同 (bucket_type,bucket,date) 幂等更新不重复；clear_bucket_bars('theme')/clear_bucket_rrg('theme') 只清 theme 行、sector 行保留；read_bucket_rrg/bucket_names 返回正确、upsert_bucket_rrg 幂等；theme_membership_asof 在多 as_of fixture 上:早快照取改写前 exposure、晚快照见 restatement/drop(exposure=0→剔除)/late-join、未来 as_of 行不污染早快照(C3)、theme=/ticker= 过滤正确"
      }
    },
    {
      "notes": "M1.2–M1.5 web 任务与 CI offline-pipeline job 的共用离线前置；非 nightly pipeline；data/ 与 web/public/data/board.json 走 gitignore 不入库。见 export/board.py 合同与 memory m1-web-export-verification。",
      "path": "compute/fixture.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "合成 DuckDB fixture：确定性造 universe/daily_bars/spx_daily/fundamentals_q + bucket_bars(11 条合成 sector ETF 序列,离线验证 M3 rotation) + theme_membership(多 as_of point-in-time membership,离线验证 M4),供无网络环境(CI/sandbox,真实 ingest 依赖 api.nasdaq.com + Yahoo 常不可达)离线验证 compute/export/web —— 经 compute/db.py 真实 upsert 落库后喂真实引擎,验证生产读路径而非造数",
        "invariants": "写五张字节稳定源表(universe/daily_bars/spx_daily/fundamentals_q + bucket_bars) + theme_membership、绝不写 derived_daily/valuation_daily(那是引擎的活,须跑 make compute 物化)；经 compute/db.py 的 upsert_* 落库即与真实 ingest 同一写入路径、不自绕 schema；numpy 单一 seed 按固定顺序消费 + 固定 end-date 锚点(非 today) → 给定 (seed,tickers,days,end-date) 字节级可复现；合成 11 sector ETF 经 db.upsert_bucket_bars 落 bucket_bars(bucket=GICS 名)、绝不进 universe 横截面、且造数放在所有现有 rng 消费之后(尾部 draw)；theme_membership 由 THEME_PLAN 确定性生成(无 rng draw)、置于 bucket_bars 之后 → 上述五表 daily_bars/spx_daily/fundamentals_q/bucket_bars/universe 保字节不变(M1/M2/M3 提交版 fixture 依赖此);theme 造数覆盖 AC-M4 全部 PIT 机制:≥4 theme、2 个 as_of、多对多(TT01∈AI+SEMI)、exposure restatement、late-join、drop-via-exposure=0,exposure∈[0,1] 连续、approved_by 必填(C6);内建边缘票 eps<0/ebitda<0/stale/overdue/无基本面,默认 30 票×320 日；属产品代码用 venv 依赖(numpy/duckdb,非 engine stdlib-only 范畴)；dev/CI 工具非 nightly pipeline 成员；默认 fresh-build data/tickertide.duckdb(gitignored 派生物)只含合成数据",
        "verification": "make fixture 产出五源表非空(bucket_bars=11 sector×days) + theme_membership(默认 13 行,4 theme,2 as_of)；make fixture-pipeline 后 compute/check.py 输出 CHECK_OK(AC-M0 八项 PASS)、export/board.py composite_recon_max_drift≈0(C9)；同 seed 两次重建五表 sha256 一致、且扩 theme_membership 前后五表(universe/daily_bars/spx/fundamentals/bucket_bars)sha256 不变(回归);PIT 断言:db.theme_membership_asof 早快照=改写前 exposure、晚快照见 restatement/drop/late-join、未来 as_of 不污染早快照(C3)、exposure∈[0,1]、approved_by 非空"
      }
    },
    {
      "notes": "",
      "path": "compute/ignition.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "per-stock ignition 信号数学（早期发现引擎，PRD §10.8.1，与 composite 并列的第二台）：5 个短窗口分量（动量加速 ret10/10-ret50/50 / 波动收缩-扩张 |Δp|10/60 / 放量 vol5/60 / 突破收复 close/max60·1[>MA50] / RS 拐点 slope10-slope30/3）",
        "invariants": "pandas 算 per-stock 时序、分量皆 self-relative（横截面 percentile-rank 在 run.py 做）；与 signals.py 共用同一份 bars 与同一 benchmark 对齐口径（C9，spx.close=adj_close basis、ffill 到本票交易日）；用 adj_close 作价；early⟷reliable 旋钮不触 ignition（PRD §10.8/P7），精度靠 persistence 非权重；窗口短于 composite（10/50/60 vs 63/126/252）测加速度/拐点非水平",
        "verification": "compute_ignition 返回 date + ig_accel/ig_expand/ig_vsurge/ig_breakout/ig_rsturn 五列；ig_breakout∈[0,1]（构造 clamp）；横截面综合后（run.py）ignition∈[0,100]；公式对齐 PRD §10.8.1 与 analysis/verify_ignition.ignition_metrics 同源"
      }
    },
    {
      "notes": "",
      "path": "compute/rotation.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "rotation compute：bucket_bars + spx_daily 周线 RS-Ratio(JdK 透明 reconstruction) → bucket_rrg(M3.2)；league_table 按 bucket_type 聚合 per-bucket 成员(breadth/at-52w-high/composite 中位/agg EV-S/多 horizon rel return)供 export。M4.3：bucket_type='theme' 整路复用(对 theme index 价做同一 RS-Ratio)，仅 league 成员源换为 theme_membership(point-in-time)",
        "invariants": "周线(daily resample 周五收盘)；RS=100×P_bucket/P_spx(price relative)、M=EMA(rs,n1)、rs_ratio=100+k·(M−SMA(M,n2))/σ(M,n2)(z-score recenter 100，>100=跑赢自身近期趋势)；momentum=rs_ratio 斜率、RS-Momentum 归一量已砍(PRD §16)；n1/n2/k 暴露 PARAMS 透明 reconstruction、不声称复刻 StockCharts/de Kempenaer；compute_rotation 只读 bucket_bars+spx_daily、绝不碰 universe 横截面(隔离 PRD §16)、且按 bucket_type 限域清 bucket_rrg(sector/theme 共表共存)；point-in-time 关键在 theme index 构建(theme_index.py，M4.2)非此处 z-score —— rotation 对 theme index 价做的 RS-Ratio 算法与 sector 完全同一(不翻案)，sector z-score temporal、theme 的 PIT 已由 index 序列承载；league 成员源由 bucket_type 决定 —— sector→universe.sector、theme→theme_membership_asof(point-in-time、exposure>0、多对多：一票可计入 AI+SEMI 两行)，成员量(breadth/at-high/composite 中位/agg_evs)取自 derived_daily/valuation_daily 同源可追溯(C9)、不持久化、不造成员卡(走 board.json，DRY)；infer_state/slope_4w 是 state/Δ4w 单一口径",
        "verification": "make fixture-pipeline + theme_index 后 python3 compute/rotation.py --bucket-type theme：bucket_rrg theme ≥4 周序列、rs_ratio ~95-105、4 态可推；theme league member_count 与 theme_membership_asof(latest)逐 theme 一致、composite_median 与直接查 derived_daily 该 theme PIT 成员 median 逐位一致(C9)、TT01 计入 AI+SEMI(多对多)；clear 限域：跑 sector 再跑 theme 后 bucket_rrg 仍含 11 sector + 4 theme(共存)；M3 回归：sector 路径 make rotation-c9 仍 GATE_PASS"
      }
    },
    {
      "notes": "",
      "path": "compute/run.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "M0.3+M7.1 compute 编排：daily_bars + spx_daily → derived_daily（横截面 rs_pct + composite + ignition）",
        "invariants": "pandas 算 per-stock（signals 长窗 + ignition 短窗，共用同一份 bars，C9）、DuckDB 算横截面 PERCENT_RANK/rank；composite 分量 c_* 存原始供 client 按任意 k 重配权、composite 仅默认 k 快照；ignition=100·mean(5 分量各自横截面 percentile-rank)、ign_pct=ignition 的横截面 percentile、ign_persist_days=连续(含当日)ign_pct≥90 的天数(islands 技法,非 lit 行=0)；clear+重算幂等",
        "verification": "compute_all 后 derived_daily 非空；composite 5 分量∈[0,1]、composite∈[0,100]；ignition∈[0,100]、ign_pct∈[0,100]、ign_persist_days≥0；smoke 打印 top-by-composite 与 top-by-ignition 方向合理"
      }
    },
    {
      "notes": "",
      "path": "compute/signals.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "per-stock 信号数学：EWMAC / KER trend / 双窗 RS / MA / volume + early⟷reliable weights(k)",
        "invariants": "pandas 算时序；composite 分量∈[0,1]；weights 对每个 k 恒 Σ=1 且不删任何分量；math 对齐 PRD §10",
        "verification": "compute_metrics 返回各 metric 列；weights(0)/weights(1) 之和均=1"
      }
    },
    {
      "notes": "",
      "path": "compute/theme_index.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "theme 指数(M4.2)：theme_membership(point-in-time) + daily_bars → bucket_bars(bucket_type='theme')，每 theme 一条日价序列。非市值加权(exposure-weighted + cap,C4) + chained-return point-in-time(C3)。供 compute/rotation.py(bucket_type='theme',M4.3)算 RS-Ratio —— theme 复用整套 M3 bucket 框架,不建第 6 surface,RS-Ratio 算法不在此翻案",
        "invariants": "非市值加权(C4)：权重纯由 exposure(营收份额)派生、零 mktcap 输入；water-fill cap 使单票权重≤themes.yaml 的 cap(防单票主导,「AI」≠NVDA 一只);成员数<ceil(1/cap)时 cap 不可行 → 退等权(最分散可行向量,仍 mktcap 无关)。point-in-time(C3)：index 为 chained-return 序列(level[t]=level[t-1]·(1+Σ wᵢ·rᵢ))非加权价位 —— 当日 return 用『前一日 as-of』成员权重(db.theme_membership_asof),故改晚 as_of 只动其后、每个更早 index 值 byte 不变(theme RS-Ratio 线/Ocean trail 不虚构)、rebalance 不跳价(链式连续)、用 return 而非价位使尺度无关(高价/低价票按权重非价位贡献);index 从该 theme 首个 as_of 起 —— 更早需用今天 membership 回溯=lookahead(C3 禁),故单 as_of seed 几无历史,真实 PIT 历史靠各历史 filing 打戳的 membership(M4.5);只读 daily_bars+theme_membership、写 bucket_bars(theme)、绝不进 universe 横截面(同 sector ETF 隔离 PRD §16);clear_bucket_bars('theme')先清再全量重建;cap 取自 themes.yaml(缺省 0.30)",
        "verification": "make fixture 后 python3 compute/theme_index.py 产 ≥4 theme × N 日 bucket_bars(bucket_type='theme')、base=100 起、全正无 NaN、sector 行不受影响;capped_weights 单测:>=4 票可行时主导成员被 cap 至 cap(NVDA-like 不主导)、cap 不可行(cap×n≤1)退等权、和为 1;index 权重 == capped_weights(纯 exposure)(零 mktcap:fixture AI@last 最大市值票 mktcap 占比≈0.65 但 index 权重≤cap=0.30);C3:改晚 as_of 一条 membership→rebuild→≤late 段 index byte 不变、>late 段变;compute_rotation(bucket_type='theme')能据此产 bucket_rrg(M4.3 衔接 smoke 通过)"
      }
    },
    {
      "notes": "",
      "path": "compute/valuation.py",
      "kind": "script",
      "module": "compute",
      "stage": "implemented",
      "contract": {
        "purpose": "M0.4 估值：daily_bars ASOF JOIN fundamentals_q → valuation_daily（日频倍数 + common-vintage）",
        "invariants": "ASOF 键用 filed_date（point-in-time，非 period_end，anti-lookahead）；分子日频、分母按 filing 阶进；E≤0→n.m.(pe NULL 退 P/S)；EV=shares×close+debt−cash；common-vintage percentile 只在 fresh cohort(as-of≤~95d)内排名、stale 排除，升序故 0=最便宜(Ocean y 底=便宜)；common_vintage(con,metric,on_date,fresh_days) 为该百分位的单一定义、按交易日参数化，M2 Ocean(周度)与 M5 Valuation(横截面)复用同一函数、口径不分叉(C9)，latest_common_vintage 为其最新日 int 取整薄包装",
        "verification": "smoke 倍数量级合理(AAPL P/E~38 P/S~10、SNOW n.m.退P/S)；as_of_filed≤date 无 lookahead；common-vintage fresh/stale 分离；common_vintage 任意 on_date 只对 fresh cohort 排名、metric 经白名单校验、百分位随 ps 升序(底=便宜)"
      }
    },
    {
      "notes": "",
      "path": "devtopology.yaml",
      "kind": "config",
      "module": ".",
      "stage": "implemented",
      "contract": {
        "purpose": "devtopology 引擎配置：ledger 路径、exclude、保护分支、worktree、fill 阶段",
        "invariants": "必须是引擎简易 yaml 可解析的扁平 key + 列表；exclude 含产品数据产物；ledger 指向 docs/runtime/File-Contracts.json",
        "verification": "make atlas/verify 正确读取配置；exclude 的数据产物不进 mirror"
      }
    },
    {
      "notes": "",
      "path": "docs/BUILD-PLAN.md",
      "kind": "doc",
      "module": "docs",
      "stage": "verified",
      "contract": {
        "purpose": "构建计划 + 数学规格原件（kickoff prompt），PRD 的来源文档",
        "invariants": "为 PRD 的出处；与 PRD 冲突时以 PRD §16 已定死为准；不承载实现状态",
        "verification": "PRD 各节可追溯回本文对应章节"
      }
    },
    {
      "notes": "",
      "path": "docs/PRD.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "产品需求权威规格（SoT）：范围、原则、架构、UX、数学、FR/NFR、里程碑、验收",
        "invariants": "FR/NFR 可追溯；与 equity-monitor-v2.jsx UX 与 §10 数学一致；活文档，需求/边界变化先回写本文",
        "verification": "FR 覆盖五 surface + 引擎；不可妥协约束与 §7 一致；配色表与 UX 合同一致"
      }
    },
    {
      "notes": "",
      "path": "docs/ROADMAP.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "M0–M6 开发方案：PRD §13 里程碑的执行展开（M0–M4 + D 轨详细到可 task-open 执行，M5–M6 纲要）；进度行是各 M/PR 完成态的 SoT",
        "invariants": "引用 PRD/BUILD-PLAN 不复制；M0–M1 拆解可追溯到 §13/§14、M2–M6 纲要；just-in-time 临近再细化、不提前承诺 §17 未决决策；冲突以 PRD §16 为准",
        "verification": "M0–M1 各 task 与对应 AC 一致；M2–M6 纲要指向 PRD §14；依赖链自洽"
      }
    },
    {
      "notes": "",
      "path": "docs/agent-state/README.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "三层状态协议入口：L1 Git / L2 运行时 / L3 任务态",
        "invariants": "是协议/索引非日志；保持简短；用 live readback 命令读真值不复制会过期状态",
        "verification": "每层给出的命令可跑；指针指向真实文件"
      }
    },
    {
      "notes": "",
      "path": "docs/equity-monitor-v2.jsx",
      "kind": "doc",
      "module": "docs",
      "stage": "verified",
      "contract": {
        "purpose": "UX 合同 mock，锁定 layout/interaction/hierarchy/color",
        "invariants": "是视觉契约非实现；用合成数据；code 不得照抄其造数逻辑；冲突以 PRD §16 为准",
        "verification": "PRD §9 与附录 C 的布局/字段/配色与本文件一致"
      }
    },
    {
      "notes": "",
      "path": "docs/runtime/Credentials-Management.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "凭证平面模型与生命周期（advisory）：平面边界、轮换、incoming-staging 流程",
        "invariants": "names-only；每个 runtime secret 属唯一平面、平面有硬边界；runtime secrets 不入仓库",
        "verification": "Env-Registry 与 Secrets-Inventory 模板引用本模型的平面标签"
      }
    },
    {
      "notes": "",
      "path": "docs/runtime/Deploy-DuckDB-Wasm-R2-Runbook.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "M5.2 R2 部署 runbook：duckdb-wasm bundle 托管到 Cloudflare R2 的一次性 operator 设置(bucket/公开域/CORS/上传/repo 变量)",
        "invariants": "为什么 R2:wasm 35-41MB 超 Pages 25MiB 单文件限制;R2 是用户自有对象存储非第三方 CDN(脊柱:parquet 仍自托管、仅静态 wasm lib 外移);未配 R2 时 client fallback jsDelivr CDN(site+nightly 安全);wasm 绝不入 web/dist(URL 加载);跨域 worker 用 blob importScripts;DUCKDB_R2_BASE 是公开 URL 用 repo 变量、R2 token 只在本地",
        "verification": "按 runbook 设置后 nightly 部署的 Valuation tab 浏览器从 R2 base fetch wasm 200(CORS ok)、渲染全 universe;wasm 请求走 R2 非 dist"
      }
    },
    {
      "notes": "",
      "path": "docs/runtime/Deploy-Pages-Runbook.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "D.3 部署操作 runbook：Cloudflare Pages 生产公开上线的凭证两条路、建项目、首次 go-live、可选预览 Access 保护、CI 自动部署、AC-D 验收清单、回滚",
        "invariants": "只记操作步骤与凭证位置(names-only)，绝不含 token/account 值；生产 tickertide.pages.dev 公开无登录，预览 wildcard 可由 Cloudflare Access email 白名单保护；数据源条款复核按 PRD §6.2/NFR-7；部署只在 nightly 全绿后(陈旧/失败不发布)；与 Credentials-Management.md / nightly.yml / wrangler.jsonc 三者一致",
        "verification": "按 runbook 走通首次 go-live + 公开生产访问验证(AC-D 清单可全勾)；grep 确认无 secret 值入库"
      }
    },
    {
      "notes": "",
      "path": "docs/runtime/Env-Registry.example.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "names-only env 登记表模板",
        "invariants": "只记 env_key/source/consumer 等名称，绝不记值；真实 Env-Registry.md 走 gitignore",
        "verification": "模板内无任何值；真实文件被 .gitignore 忽略"
      }
    },
    {
      "notes": "",
      "path": "docs/runtime/File-Contracts.json",
      "kind": "doc",
      "module": "docs",
      "stage": "verified",
      "contract": {
        "purpose": "文件合同账本：每个源文件一条 purpose/invariants/verification 合同，是 devtopology gate 的单一真源",
        "invariants": "每个非排除仓库文件有且仅有一条 entry；stage 取合法值；自身在 fill_exempt 中故不被 semantic gate 检查占位；增删文件后须 make writeback-apply WRITE=1 同步",
        "verification": "make verify 的 structure_contract_coverage 报 missing=0 stale=0"
      }
    },
    {
      "notes": "",
      "path": "docs/runtime/Manifest-Contract-System.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "文件合同系统参考：合同字段、stage 体系与 gate 语义",
        "invariants": "与 engine/index.py 实际行为及 stage 取值一致",
        "verification": "stage_values 与 engine POLICY_STAGE_VALUES 一致；字段与账本 schema 匹配"
      }
    },
    {
      "notes": "",
      "path": "docs/runtime/Real-Ingest-Notes.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "D.1 真实 ingest 冒烟笔记：真实数据三源状态 + 坑/修复(sector 归一 / Yahoo 429 yfinance 库绕过 / EDGAR concept) + 规模时间 + D.2/D.3 生产上线解锁",
        "invariants": "记录首次真实数据端到端(2026-06-05)：AC-M0 真实 CHECK_OK + rotation-c9/ocean-c9 PASS；坑清单有明确消费者(D.2 nightly 配置 / 真实 ingest debug)；secrets 走 Actions secret 不入库；names/counts only 无 secrets",
        "verification": "对照 D.1 真实 pipeline+export 输出；nasdaq.py NASDAQ_TO_GICS 归一与本 note 表格一致"
      }
    },
    {
      "notes": "",
      "path": "docs/runtime/Secrets-Inventory.example.yaml",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "运营资产 + 凭证清单模板（含轮换时间戳占位）",
        "invariants": "仅为模板；真实 Secrets-Inventory.yaml 存于 git 外并被 gitignore",
        "verification": "真实 inventory 文件被 .gitignore 忽略；模板不含真实凭证"
      }
    },
    {
      "notes": "",
      "path": "docs/workflow/WORKFLOW.md",
      "kind": "doc",
      "module": "docs",
      "stage": "implemented",
      "contract": {
        "purpose": "工作流 policy：权威顺序、merge 边界、结构门禁、验收标准、自动合并、decision gate",
        "invariants": "PR 为唯一合并边界；acceptance comment 为验收 SoT；薄 policy 指向 SKILL recipe；命令与 Makefile 一致",
        "verification": "所列命令与 Makefile 一致；与 CLAUDE.md/AGENTS.md 不矛盾"
      }
    },
    {
      "notes": "",
      "path": "engine/index.py",
      "kind": "script",
      "module": "engine",
      "stage": "verified",
      "contract": {
        "purpose": "devtopology 核心引擎：mirror/atlas/contracts/writeback/gates 流水线",
        "invariants": "Python stdlib only 零外部依赖；gate 输出统一 GATE_PASS/FAIL 结构化行；不耦合任何业务域",
        "verification": "make verify/mirror/atlas 可跑且无外部 import；占位常量与文档一致"
      }
    },
    {
      "notes": "",
      "path": "export/README.md",
      "kind": "doc",
      "module": "export",
      "stage": "planned",
      "contract": {
        "purpose": "静态分发分片模块规划：snapshot → Parquet/JSON shards",
        "invariants": "导出物不含任何 key；依赖 membership 的 point-in-time 正确性；分片粒度为实现级决策",
        "verification": "输入/输出与 PRD §5、§12 一致"
      }
    },
    {
      "notes": "themes[] 在 theme_membership 表存在前恒空(M4)；board.json 走 data/ gitignore 规则不入库；真数据由 `make pipeline` 物化，本任务以合成 fixture 喂真实引擎验证",
      "path": "export/board.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "M1.1/M7.2 导出：DuckDB 最新快照 → web/public/data/board.json（Discovery evidence-first 卡流数据，per-stock 一条）+ theme chips（point-in-time，M4.4）+ ignition 发现引擎块（持续点火排序 + 点火证据，M7.2）",
        "invariants": "每票携带引擎 composite + 5 个原始分量 c_*（前端按旋钮 k 重配权、绝不在导出端重算引擎 — C9）；C9 自检以 signals.weights(k) 重构 100·Σwᵢ·cᵢ 必须复现 derived_daily.composite，漂移>tol 即硬失败；evidence 6 字段(ret_1m/3m/6m·from_high·weeks_since_breakout·vol_mult)+90d mini chart+composite 同源一份 export 可互相追溯；ret_1m/vol_mult/weeks_since_breakout 由 bars 派生(非引擎信号；weeks_since_breakout 为透明 252d-high proxy)；valuation 暴露 as_of 新鲜度分档(fresh≤95d/stale≤160d/overdue)、E≤0→pe null 退 P/S、point-in-time；composite_prev 提供 d/d；theme chips 经 db.theme_membership_asof 取 as-of snapshot 的 point-in-time membership(per theme 最新 as_of≤snap 且 exposure>0、多对多、按 exposure 降序)、绝非旧 naive 全历史扫描(多 as_of 票每 theme 仅一条不重复)、exposure 存 [0,1] 份额卡渲染为 %；ignition 块为第二台引擎(发现，PRD §10.8)逐票携带：ignition∈[0,100]/ign_pct∈[0,100]/ign_persist_days/5 个原始分量 ig_*(accel/expand/vsurge/breakout/rsturn) 全部 verbatim 自 derived_daily 绝不在导出端重算(C9，与 compute/run.py 同源)，candidate=ign_pct≥90 AND persist≥ignition_persist_min(=5，持续点火过滤 §10.8.2，前端 M7.3 据此排序而非 composite)，early⟷reliable 旋钮不触 ignition(P7)；点火证据(breakout_day/days_since_breakout/step_rate_ratio/reclaimed_ma50/ma50)由 ignition 同窗口 bars(BREAKOUT_WIN=60、ACCEL 10/50) 派生、vol_mult× 为 ig_vsurge verbatim；ignition C9 自检以 shipped vol_mult 追 stored ig_vsurge，漂移>tol 即硬失败；输出为派生物、gitignore、不含任何 key",
        "verification": "compute+valuation 后 `python export/board.py` 成功；board.json 顶层 composite_recon_max_drift≈0 且 ignition_recon_max_drift≈0(C9)；每条含 composite+5分量+6字段+90d bars+valuation+composite_prev+ignition 块(score/ign_pct/persist/5 分量/candidate/点火证据)；抽查 candidate 票 ign_pct≥90 且 persist≥5 可排序在顶、点火证据 vol_mult==分量 vsurge、ignition∈[0,100]、ign_persist_days≥0、无 NaN token；E≤0 票 pe=null 且 ps 非空、freshness 分档正确、rank 连续且 composite 降序；theme chips point-in-time：多 as_of 票每 theme 一条非重复、改写后取最新 exposure、drop(exposure=0)不出 chip、多对多(TT01∈AI+SEMI)"
      }
    },
    {
      "notes": "读单份 board.json(export/board.py 产物)做 ignition 块的同源自检；本任务以 30 票 fixture 验证 coverage=30/candidates=2(persist≥5)/vsurge_checked=30 全过；负向(翻 candidate 标记 / 改 vol_mult)可触发 GATE_FAIL",
      "path": "export/check_ignition.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "M7.2/M7.4 ignition 同源自检：board.json 的 ignition 块可追溯到引擎(C9)+ 持续点火 candidate gate 自洽 + M7.4 stock bundle 与 board 同源（AC-M7「ignition↔composite 同源、Discovery 按持续点火排序、持续点火榜非空可追溯」）",
        "invariants": "读 export/board.py 产出的 board.json，对 ignition 块逐票断言：每票有 ignition 块且 components 恰 5 个(accel/expand/vsurge/breakout/rsturn)；ignition∈[0,100]、ign_pct∈[0,100]、ign_persist_days≥0(PRD §10.8.2)；candidate 严格 == (ign_pct≥90 AND persist≥board.ignition_persist_min) —— 由 shipped 数字重算、不信任导出端的标记；点火证据 evidence.vol_mult 必 == 分量 vsurge(ig_vsurge verbatim 的同源 identity guard，容差仅取整 1e-3)；顶层 ignition_recon_max_drift≈0；至少 1 个持续点火 candidate(否则 Discovery 榜空)；M7.4 加 stock↔board 交叉校验：抽样 per-name stock bundle(export/stock_bundle.py)的 ignition 块必逐字段 == board 同票(两者同一 _ignition builder/同源 derived_daily，跨 surface 不漂移)，--stock-dir 缺则跳过；任一不符 GATE_FAIL 非零退出可门禁；names/counts only 无 secrets",
        "verification": "make ignition-c9（先 fixture-pipeline + export board + stock bundles）→ GATE_PASS、coverage/candidates/vsurge_checked/stock_xcheck 计数一致；构造不符(翻 candidate 标记 / 篡改 vol_mult / 越界 ign_pct / stock bundle ignition 漂移)可触发 GATE_FAIL"
      }
    },
    {
      "notes": "跨两份 export(board.json+ocean.json，须同一 DB)校验；本任务以 520 票 fixture 验证 shared=517 全过",
      "path": "export/check_ocean.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "M2.5 C9 跨 surface 校验：ocean.json 点位可追溯到 Stock/Discovery 数字（AC-M2「Ocean 点位与 Stock 数字一致」）",
        "invariants": "读同一 DB 导出的 board.json + ocean.json，对最新快照逐票断言：as_of 同日；rs=ocean.pts[-1].rs == board.components.rs×100(同 derived_daily.rs_pct)；ps=ocean.pts[-1].ps == board.valuation.ps(同 valuation_daily.ps)；容差仅取整(rs 0.1/ps 0.01)；只比两份都在的票；任一不符 GATE_FAIL 非零退出可门禁；names/counts only 无 secrets",
        "verification": "make ocean-c9（先 fixture-pipeline + make export 出两份）→ GATE_PASS、shared/rs_checked/ps_checked 计数一致；构造不符可触发 GATE_FAIL"
      }
    },
    {
      "notes": "",
      "path": "export/check_rotation.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "C9 跨 surface 校验(AC-M3/AC-M4)：rotation league 与 board.json 成员聚合一致；按 rotation 文件 bucket_type 分组 board 成员(sector→universe.sector / theme→board theme chips)，gate sector(rotation.json)与 theme(rotation.theme.json)两导出",
        "invariants": "同一 DB 两 export 比对最新快照：as_of 一致、rotation.members[]⊆board.tickers(成员卡可 scope filter 追溯)、member_count==board 同 bucket 成员数、composite_median==median(board 同 bucket composite)；theme 分组按 board 每票 themes chips(多对多：一票计入其每个 PIT theme)、故 theme league PIT 成员须与 board PIT theme chips 一致(闭环 theme C9)；退出码 gate(GATE_PASS/FAIL)；names/counts only 无 secrets；board.py --min-bars 可能裁剪薄历史票致 member_count 差(真实数据前需对齐，已注释标注)",
        "verification": "make rotation-c9(sector)：GATE_PASS、member_count/composite_median 11 sector 逐一致；theme：python3 export/check_rotation.py --rotation web/public/data/rotation.theme.json GATE_PASS(4 theme member_count/composite_median 逐一致、members⊆board)；mismatch 退非 0"
      }
    },
    {
      "notes": "",
      "path": "export/check_valuation.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "M5.5 AC-M5 + C9 验收：valuation.parquet/stock bundle 追溯到 board.json(同一引擎)",
        "invariants": "只读、CI 友好(任一问题 exit 非0)；duckdb 读 parquet 验列齐(pe/ps/.../peg/margin/freshness/themes)+freshness∈{fresh,stale,overdue,NULL}；C9 valuation↔board 同票 ps/pe 一致(同 valuation_daily，tol 0.02)；C9 stock↔valuation 抽 8 票 bundle ps_series 末值==screener ps；前端行为(duckdb-wasm 排序/三档色/pctile/四格)由 web 测试+浏览器 smoke 覆盖、本 check 只验数据侧；valuation.parquet 缺失 SKIP exit 0",
        "verification": "make valuation-c9 对 fixture export 输出 GATE_PASS、c9_valuation_board=全票、c9_stock_valuation 抽样；gate.yml offline 与 nightly 串入"
      }
    },
    {
      "notes": "",
      "path": "export/manifest.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "D.4 新鲜度 manifest：读 board/ocean/rotation/rotation.theme.json → manifest.json(最新 as_of + 各 surface count)，供 web as_of badge 免 load 全量 JSON + D.3 deploy gate",
        "invariants": "读 export 已写的 board/ocean/rotation/rotation.theme(M4.6)+valuation(valuation.meta.json)/stock(stock/index.json)的 count/as_of(M5.5 EXTRA sidecar，非 <name>.json)；as_of_date 取各 surface 最新；缺 surface→None(部分 export 可见、非静默当完整)；Makefile export 末尾跑(在三 export 后)；输出 gitignored 紧凑 JSON",
        "verification": "make export 后 manifest.json 含 as_of+surfaces counts；as_of==max(各 surface as_of)；缺文件→对应 surface=None"
      }
    },
    {
      "notes": "themes[] 在 theme_membership 表存在前恒空(M4)；ocean.json 走 data/ gitignore 规则不入库；真数据由 `make pipeline` 物化，本任务以合成 fixture 喂真实引擎验证；Makefile export 接线在 M2.5",
      "path": "export/ocean.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "M2.1 导出：DuckDB 周度快照 → web/public/data/ocean.json（Ocean wide-explore 散点每票 N 周 RS×估值位置序列，供 scrubber + pin trail）",
        "invariants": "每票 pts[] 与 weeks[] 对齐(老→新)：rs=derived_daily.rs_pct(0-100 横截面，与 Discovery/Stock 同列同源、点位可追溯 C9)、val=common-vintage 百分位(0-100，底=便宜，§10.5)、ps=val 背后的原始倍数(供 hover tip §9.2，与位置同周)；val 一律经 compute.valuation.common_vintage 计算(M2 Ocean 与 M5 Valuation 复用同一函数、口径不分叉 C9)；weeks=各 ISO 周最后交易日(date_trunc('week'))、最新周==最新快照(==board.json as_of)故 Ocean 当前位与 Stock 数字一致；入选票=最新周同时有 rs 且 fresh val(有可渲染 (x,y))，stale/n.m./无基本面者无 y 坐标→不导出(符合 §10.5 stale 不进 percentile)，历史周缺 rs/fresh-val 则该 pt=null(不伪造位置)；mktcap 原始 USD(同 board.json)；输出为派生物、gitignore、不含任何 key/secret",
        "verification": "fixture-pipeline 后 `python export/ocean.py` 成功；self-check 硬失败守护(每票 len(pts)==n_weeks 且最新 pt 非空；val orientation 最便宜 ps 的 val 不高于最贵=底=便宜)；ocean.json rs/val∈[0,100]、ps>0、weeks 老→新且 weeks[-1]==max(derived_daily.date)；none/stale/overdue profile 票缺席；≥500 票 fixture 下 count≥500、size 几百 KB、export<1s"
      }
    },
    {
      "notes": "",
      "path": "export/rotation.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "rotation 导出：DuckDB → web/public/data/rotation.json(sector,M3.3) / rotation.theme.json(theme,M4.4)（RS-Ratio 多线序列 + enriched league，每 bucket 一条，供多线图 + league 表 + 钻进）",
        "invariants": "读 bucket_rrg(周线 RS-Ratio 序列)对齐共享 weeks[](最近 N 周 union、缺失补 null，仿 ocean pts)；as_of_date 取实际最后交易日(max(derived_daily.date)，与 board/ocean 同源 C9)非 weeks[-1] 周线标签(后者 mid-week 跑领先成未来周五、破 C9 + 污染 D.4 badge)，weeks[] 仍用周五标签做 x 轴；league 量经 compute.rotation.league_table 取自 derived_daily/valuation_daily 成员(与 Discovery/Ocean/Stock 同源 C9)；members 经 rotation._bucket_members 取 top-N ticker(sector→universe.sector / theme→theme_membership point-in-time 多对多；成员卡走 board.json scope filter 不重复造 DRY)；sector 附 etf(sector_etf_map.txt 审计)、theme 无 etf(None)；sector 与 theme 各写一文件(各自 weeks 轴/params；theme index 从首个 as_of 起故 weeks 可少于 sector)、web 按 GICS↔Theme toggle 载对应文件；params 暴露 n1/n2/k(透明 reconstruction 不复刻 StockCharts)；inline self_check(rs_ratio 对齐 weeks、weeks 老→新、末值非空)；输出 gitignored 紧凑 JSON；同 fixture seed 字节级可复现",
        "verification": "make fixture-pipeline(+theme_index) 后 python3 export/rotation.py [--bucket-type sector|theme]：sector→rotation.json 11 bucket weeks=52、theme→rotation.theme.json ≥4 theme(members 取自 theme PIT、无 etf)；4 态、params 暴露、rs_ratio 对齐 weeks；theme C9(check_rotation --rotation rotation.theme.json)GATE_PASS；same seed 重导出 byte 一致"
      }
    },
    {
      "notes": "",
      "path": "export/stock_bundle.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "M5.3 Stock per-name bundle 导出：每 universe 票 → web/public/data/stock/<TICKER>.json(+index.json)，为 M5.4 时间轴对齐 stack 提供时序；M7.4 起并带 ignition 块(第二引擎，PRD §10.8)供 Stock 点火诊断",
        "invariants": "每票一 JSON 分片、懒加载(Stock narrow 一次一票，无需 duckdb-wasm)；含 price(~2y/504d 日 OHLCV+MA50/150/200+52wh)+revenue_q(季度 TTM 营收+YoY，PRD §9.6 REVENUE bars)+ps_series(每日 P/S，§9.6 P/S over time)+header(ticker/name/sector/mktcap/composite/themes PIT)+valuation 卡+5 分量(composite)，per-name 自给(任意 universe 票，非 board top-N)；M7.4 加 ignition 块(可空)：verbatim 自 derived_daily、由 export/board.py 的 _ignition 同一 builder 组装(勿重算，C9)——含 ignition/ign_pct/ign_persist_days/candidate + components(5 原始 self-relative)+evidence(点火证据)，与 board.json 同票 ignition 块逐字段一致；price 与 ignition 证据共用同一份 bars(_bars 一次取)；读同一 daily_bars/valuation_daily/fundamentals_q(C9 同票 P/S 与 Ocean/Valuation 一致)；index.json 列 ticker+as_of(Stock 下拉/manifest)；输出 gitignored",
        "verification": "make export 产 stock/<T>.json 全 universe + index.json；抽查 bundle 含 price/revenue_q(YoY)/ps_series/valuation/components/themes/ignition；ps 与 valuation_daily 一致、ignition 块与 board.json 同票逐字段一致(C9，make ignition-c9 的 stock↔board 交叉校验)"
      }
    },
    {
      "notes": "",
      "path": "export/valuation_parquet.py",
      "kind": "script",
      "module": "export",
      "stage": "implemented",
      "contract": {
        "purpose": "M5.1 估值横截面导出：全 universe 最新 valuation 快照 → web/public/data/valuation.parquet（+ .meta.json），供 M5 Valuation screener 用 duckdb-wasm 浏览器查询（wide explore）",
        "invariants": "全 universe（derived_daily 当日票）每票一行最新 as-of valuation；含 peg/margin（valuation_daily 自 M0 已算、board.json 未导出的两列）+ themes 列（point-in-time membership as-of snap、exposure>0、逗号连接；theme_membership 不存在则空——供 Valuation theme scope 自给）；与 board.json 读同一 valuation_daily（C9 同票一致）；snap=max(derived_daily.date) 取值后字面嵌入 SQL（避免 DuckDB correlated 列 + outer join 限制；snap 是 DB 派生 DATE 非用户输入）；freshness 三档 95/160 与 board.py 一致、period_end→snap 同尺；duckdb 原生 COPY 写 parquet 不依赖 pyarrow；输出 gitignored（夜间派生）、.meta.json sidecar 带 as_of+count 供 manifest(D.4)/前端 header",
        "verification": "make export 产 valuation.parquet（全 universe 行、含 peg/margin 列、freshness∈{fresh,stale,overdue,NULL}）+ valuation.meta.json；duckdb 读回列齐、按 ps 升序便宜在上；同票与 board.json valuation 一致（C9）"
      }
    },
    {
      "notes": "",
      "path": "gate/report.sh",
      "kind": "script",
      "module": "gate",
      "stage": "verified",
      "contract": {
        "purpose": "standalone bash gate 日志解析器，将 GATE_PASS/FAIL 行解析为结构化结果",
        "invariants": "纯 bash 无外部依赖；只读日志不改状态",
        "verification": "对 .index/health 的 verify 日志能解析出 gate 结果"
      }
    },
    {
      "notes": "",
      "path": "ingest/README.md",
      "kind": "doc",
      "module": "ingest",
      "stage": "planned",
      "contract": {
        "purpose": "数据拉取模块规划：Stooq + Nasdaq screener + SEC EDGAR (+yfinance fallback)",
        "invariants": "US-only 过滤；公开展示 / 再分发 / 商业化前复核数据源条款；输出表 schema 对齐 PRD §12",
        "verification": "职责/源/输出与 PRD §6、§12 一致；未来文件清单明确"
      }
    },
    {
      "notes": "",
      "path": "ingest/edgar.py",
      "kind": "script",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "SEC EDGAR companyfacts → fundamentals_q：trailing-4Q 财务，point-in-time",
        "invariants": "取每期最早 filed（原始首报）防 restated（anti-lookahead，PRD §10.5）；按 (start,end) 期间去重不信脏的 fy/fp；概念用 fallback 列表合并覆盖；限速 ~10 req/s 需 descriptive UA；Q4 由 annual−3 单季推导；segment 不在此（companyfacts 无 XBRL segment 维度，归 M4）",
        "verification": "smoke(AAPL/NVDA/MSFT)：rev_ttm 量级正确(416/216/282B)；filed−period_end gap 中位~30d；无 lookahead；覆盖到最新季"
      }
    },
    {
      "notes": "",
      "path": "ingest/nasdaq.py",
      "kind": "script",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "从 Nasdaq screener download 端点拉取 US 股票 universe（含 sector/industry/mktcap）",
        "invariants": "需 browser-like User-Agent 否则空响应；三交易所即 US-listed（ADR 允许）；按 ticker dedup 保留大 mktcap；sector 经 NASDAQ_TO_GICS 归一到 GICS 名(Technology→Information Technology 等 4 个分歧名、Miscellaneous 无对应保留)，使 universe.sector join rotation 的 sector_etf_map(GICS)、league 不断(D.1 真实数据修)",
        "verification": "fetch_universe 返回 ≥500 行且 sector/mktcap 多数非空；含 NASDAQ/NYSE/AMEX"
      }
    },
    {
      "notes": "",
      "path": "ingest/prices.py",
      "kind": "script",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "可插拔 price provider 接口 + 工厂（默认 yfinance，Stooq 待 apikey）",
        "invariants": "provider 返回统一 bar 元组 (date,open,high,low,close,adj_close,volume)；具体 provider lazy import；换 provider 不动 run.py；换源是 provider 选择非脊柱改动",
        "verification": "get_provider('yfinance'/'stooq') 返回实例；未知名 raise ValueError"
      }
    },
    {
      "notes": "",
      "path": "ingest/run.py",
      "kind": "script",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "M0 ingest 编排：universe(Nasdaq) → daily_bars(provider) → spx_daily",
        "invariants": "全量存 universe 但 bars 只拉 top-N(mktcap) ∪ seed（M0 先窄，扩量是 M6）；provider 失败 skip 不崩；--provider/--limit/--lookback-days 可调",
        "verification": "python3 ingest/run.py --limit N 端到端产出 DuckDB；打印 universe/bar_tickers/bars/spx 计数"
      }
    },
    {
      "notes": "",
      "path": "ingest/schema.sql",
      "kind": "code",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "DuckDB schema：universe/daily_bars/spx_daily/derived_daily/fundamentals_q/segment_revenue/valuation_daily + bucket_bars(sector ETF / theme index 日 close) + bucket_rrg(周线 RS-Ratio 序列) + theme_membership(多对多 ticker↔theme 连续 exposure,point-in-time) 建表 DDL",
        "invariants": "幂等 CREATE IF NOT EXISTS；对齐 PRD §12；derived_daily 存 5 分量供 client 重配权；fundamentals_q 以 filed_date 为 as-of；valuation_daily 暴露 as_of_period_end 供 common-vintage；segment_revenue 建表但 M0 不填(归 M4)；bucket_bars 与 universe daily_bars 硬隔离 —— ETF/index 价绝不进横截面(rs_pct/rank)，bucket=GICS sector 名(==universe.sector)供 rotation league 按名 join、close 为 adj_close(total-return，与 spx_daily 一致)、bucket_type 区分 sector(M3)/theme(M4)；bucket_rrg 存周线 rs(原始 price relative 审计)+rs_ratio(JdK z-score recenter 100)，rs_mom(RS-Momentum 归一量)已砍(PRD §16，momentum=rs_ratio 斜率即时算不持久化)；theme_membership PK=(ticker,theme,as_of_date) 即 point-in-time(C3)——同 ticker-theme 多 as_of 行=历史,改写只追新行不改旧;成员@t=per(ticker,theme) argmax(as_of≤t) 取最新行后再筛 exposure>0(exposure=0 行=as-of 退出且不回溯改写历史);exposure∈[0,1] 连续(非二元),approved_by 必填(C6,'seed' 标 bootstrap),非市值加权 cap 在 index 构建(M4.2)非此；derived_daily M7.1 扩 ignition 列(ig_accel/ig_expand/ig_vsurge/ig_breakout/ig_rsturn 5 短窗分量 + ignition[0,100] + ign_pct[0,100] + ign_persist_days≥0,PRD §10.8)与 composite 同行(C9,两引擎共用 per-stock 行)",
        "verification": "compute.db.connect 应用后十表存在(含 bucket_bars/bucket_rrg/theme_membership)；重复 connect 不报错；derived_daily 含 ig_*/ignition/ign_pct/ign_persist_days 列"
      }
    },
    {
      "notes": "",
      "path": "ingest/sector_etf.py",
      "kind": "script",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "M3.1 ingest：读 sector_etf_map.txt(GICS sector→SPDR ETF) 经 M0 同款 price provider 拉 11 个 sector ETF 的 EOD bars → bucket_bars(RS-Ratio 分子源，相对 spx_daily benchmark)",
        "invariants": "只写 bucket_bars、绝不碰 daily_bars/universe —— ETF 价硬隔离不进 universe 横截面(derived_daily.rs_pct/rank)，否则百分位漂移破 C9；bucket=GICS sector 名(==universe.sector)供 compute/rotation.py league 按名 join；取 adj_close(total-return，与 spx_daily/upsert_spx 一致)；provider 失败 skip 不崩；--provider/--lookback-days/--map 可调；真实 Nasdaq screener 的 sector 名→GICS 归一属 universe 层 follow-up、不在本脚本(离线 fixture 路径已对齐)",
        "verification": "python3 ingest/sector_etf.py 端到端落 bucket_bars 并打印 ok/skipped/rows/sectors_in_db；离线由 compute/fixture.py 合成 11 sector ETF 喂同表验证(真实 provider 本环境常不可达)；load_map 解析得 11 条且 bucket 名==fixture SECTORS"
      }
    },
    {
      "notes": "",
      "path": "ingest/sector_etf_map.txt",
      "kind": "doc",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "11 个 GICS sector → SPDR Select Sector ETF(XLK/XLV/XLF/XLY/XLC/XLI/XLP/XLE/XLU/XLRE/XLB) 映射，sector_etf.py 拉取 RS-Ratio bucket 源的清单",
        "invariants": "每行 <GICS sector 名> <ETF ticker>(ETF=末空白 token，# 起行注释)；bucket 名 == GICS sector 名 == universe.sector == compute/fixture.py SECTORS(离线 league join 对齐的单一命名基准)；11 sector 全覆盖、ETF 唯一；ETF 为 Yahoo/yfinance 符号(Stooq provider 追加 .US)",
        "verification": "sector_etf.load_map 解析得 11 条；bucket 名集合 == compute/fixture.py SECTORS；ETF 无重复"
      }
    },
    {
      "notes": "",
      "path": "ingest/stooq.py",
      "kind": "script",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "Stooq price provider 占位 —— per-symbol CSV 现需 apikey（BUILD-PLAN 后政策变化）",
        "invariants": "无 STOOQ_APIKEY 时 raise 并引导用 yfinance；apikey 从 env 读不入库；endpoint 形状待有 key 时验证",
        "verification": "未设 key 时 get_bars 抛带引导信息的 RuntimeError"
      }
    },
    {
      "notes": "",
      "path": "ingest/universe_seed.txt",
      "kind": "doc",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "主题代表票种子，保证小票即使不在 mktcap top-N 也进 M0 bars 拉取",
        "invariants": "非权威 theme membership（M4 EDGAR 锚定才是）；每行一 ticker，# 注释；仅覆盖用途",
        "verification": "load_seed 解析非空；被 run.py union 进 bars 目标列表"
      }
    },
    {
      "notes": "",
      "path": "ingest/yfinance_provider.py",
      "kind": "script",
      "module": "ingest",
      "stage": "implemented",
      "contract": {
        "purpose": "yfinance price provider —— M0 默认主干（BUILD-PLAN 的 fallback 因 Stooq 需 apikey 转主力）",
        "invariants": "非官方脆弱、视中断为常态；展平单票 MultiIndex 列；NaN→None；**跳过未收盘 bar(close 为 NULL，如当天盘中行)** 保证 EOD 完整性",
        "verification": "get_bars 返回 OHLCV 元组列表 newest last；无数据返回 []；不含 close=NULL 的行"
      }
    },
    {
      "notes": "",
      "path": "requirements.txt",
      "kind": "doc",
      "module": ".",
      "stage": "implemented",
      "contract": {
        "purpose": "产品运行时依赖声明（duckdb / yfinance / pandas / numpy / pyyaml）",
        "invariants": "不含 engine/index.py 会 import 的依赖（gate 引擎须 stdlib only）；用 venv 安装；pyyaml 仅供 themes/(theme 定义解析)、engine 不 import 故零依赖约束不破",
        "verification": "pip install -r 后 import duckdb / yfinance / pandas / numpy / yaml 成功"
      }
    },
    {
      "notes": "",
      "path": "scripts/render_acceptance_comment.py",
      "kind": "script",
      "module": "scripts",
      "stage": "implemented",
      "contract": {
        "purpose": "渲染标准验收摘要，把 head_sha 绑定到验证证据",
        "invariants": "names-only/counts-only 不含 secrets/endpoints；自动补 head_sha 与 changed_files；无 commit 时优雅降级",
        "verification": "make accept GOAL=... 输出含 head_sha、changed_files 与 verify/health 字段"
      }
    },
    {
      "notes": "",
      "path": "scripts/task_router.py",
      "kind": "script",
      "module": "scripts",
      "stage": "implemented",
      "contract": {
        "purpose": "只读任务 lane 路由：判定 docs-only/workflow-only/structure-trigger 并给分支建议",
        "invariants": "绝不修改 repo 状态；输出 DOCS_ONLY/WORKFLOW_ONLY/STRUCTURE_TRIGGER/WARNINGS；路径为 tickertide 原生无 vpn-ops 耦合；分支建议前缀与 worktree.sh/WORKFLOW §2 共用单一规则（feat/docs/chore-workflow，date-first <prefix>/<date>-<descriptor>）",
        "verification": "make route 输出上述键且格式可解析；--structural/--planning/--gate 可强制触发；建议分支前缀与 worktree.sh 同 lane 一致"
      }
    },
    {
      "notes": "",
      "path": "scripts/upload-duckdb-wasm-r2.sh",
      "kind": "script",
      "module": "scripts",
      "stage": "implemented",
      "contract": {
        "purpose": "M5.2 运维脚本：把 duckdb-wasm bundle(wasm+worker)上传到 Cloudflare R2，供 Valuation screener 运行时加载(绕开 Pages 25MiB 单文件限制)",
        "invariants": "operator 步骤、每次 duckdb-wasm 版本 bump 跑一次(wasm 静态、不随 nightly 变);从 web/node_modules/@duckdb/duckdb-wasm/dist 取 4 文件(mvp/eh .wasm + .worker.js);wrangler 或 awscli(R2 S3 endpoint)二选一;.wasm→application/wasm、worker→text/javascript;凭证只在本地/operator env、绝不入库(同 Pages token);R2_BUCKET 必填",
        "verification": "R2_BUCKET=... bash scripts/upload-duckdb-wasm-r2.sh 上传 4 文件;缺文件报错;之后设 repo 变量 DUCKDB_R2_BASE 指向 bucket 公开 base URL"
      }
    },
    {
      "notes": "",
      "path": "scripts/worktree.sh",
      "kind": "script",
      "module": "scripts",
      "stage": "verified",
      "contract": {
        "purpose": "git worktree 任务隔离：open/check/status/close",
        "invariants": "拒绝在保护分支上开工作分支；一个 worktree 一个 merge intent；check 在脏工作区失败；分支前缀与 task_router/WORKFLOW §2 共用单一规则（feat/docs/chore-workflow，date-first <prefix>/<date>-<descriptor>，产品任务默认 feat/）",
        "verification": "make task-open/status/close 可跑并产出预期状态；--kind feat/docs/workflow 分别产出 feat//docs//chore/workflow- 分支前缀"
      }
    },
    {
      "notes": "",
      "path": "skills/devtopology/SKILL.md",
      "kind": "doc",
      "module": "skills",
      "stage": "implemented",
      "contract": {
        "purpose": "devtopology skill：合同/gate/drift/scope 的操作 recipe",
        "invariants": "命令与 Makefile target 一致；引用真实仓库路径；不留占位",
        "verification": "所列命令均为真 target；Key files 表与实际文件对应"
      }
    },
    {
      "notes": "",
      "path": "skills/workflow-system/SKILL.md",
      "kind": "doc",
      "module": "skills",
      "stage": "implemented",
      "contract": {
        "purpose": "workflow-system skill：PR/任务路由、结构门禁、验收摘要的操作 recipe",
        "invariants": "适配 tickertide 路径无 workrepo/.codex 残留；指向 docs/workflow/WORKFLOW.md policy；命令为真",
        "verification": "无 vpn-ops 专属路径；所列命令均为有效 make target"
      }
    },
    {
      "notes": "",
      "path": "themes/README.md",
      "kind": "doc",
      "module": "themes",
      "stage": "planned",
      "contract": {
        "purpose": "语义主题分类模块规划：LLM + EDGAR 营收锚定 membership",
        "invariants": "point-in-time membership 硬要求；主题指数非市值加权；LLM 为候选生成器+抽取器，human-in-loop 审批",
        "verification": "方法与约束与 PRD §8、§7 一致；输出表对齐 §12"
      }
    },
    {
      "notes": "",
      "path": "themes/approved/NVDA.json",
      "kind": "config",
      "module": "themes",
      "stage": "implemented",
      "contract": {
        "purpose": "M4.5 demo 交付：NVDA 人审主题 membership(durable SoT，nightly land 重播)",
        "invariants": "入 git 持久(DB 夜间重建后由 land.py 重播)；rows=AI 0.90 + SEMI 1.00(营收锚定，源 FY2026 10-K Data Center 占比/全营收皆芯片)，approved_by=sejonep(C6 人审 2026-06-13)，as_of_date=2026-02-25(=10-K filing 日，PIT 信息可得日；使最新 EOD board 即显 approved 而非回落 seed)；AI/SEMI 重叠是 PRD §8.2 多对多 feature 非 bug；provenance 记 LLM model/filing/per-row llm_exposure 供审计",
        "verification": "make themes(land) 后 NVDA 的 AI/SEMI 进 theme_membership；make check-land PIT 优先级 PASS；board PIT(as_of≥2026-02-25)解析 NVDA→AI 0.90+SEMI 1.00(非 seed 0.6)；export/board.py 后 NVDA 卡带 approved AI/SEMI chip"
      }
    },
    {
      "notes": "",
      "path": "themes/check_land.py",
      "kind": "script",
      "module": "themes",
      "stage": "implemented",
      "contract": {
        "purpose": "AC-M4.5 验收：land + point-in-time 优先级离线自检(over themes/approved/*.json)",
        "invariants": "自包含(临时 DB，db.connect 自动建 schema)→CI 无网可跑；对每条 approved 行验 C6(approved_by 非空且 source≠seed) + C3 forward(审批日 PIT 解析出 approved 值即便存在更早 seed 行) + C3 backward(审批前一日仍解析旧 seed、不回溯)；任一失败 CHECK_FAIL exit 1",
        "verification": "make check-land 对 approved/NVDA.json 输出 CHECK_OK 全 PASS；gate.yml offline-pipeline 在 fixture 链后跑此检查"
      }
    },
    {
      "notes": "",
      "path": "themes/extract.py",
      "kind": "script",
      "module": "themes",
      "stage": "implemented",
      "contract": {
        "purpose": "M4.5 LLM 主题抽取：拉 EDGAR 最新 10-K business/segment 文本 → claude CLI(print 模式,订阅 plan 额度) → 候选主题 + 营收锚定 exposure → themes/candidates/<TICKER>.json",
        "invariants": "LLM 是候选生成器+营收抽取器、绝非权威(C6)：输出仅候选待人审(themes/review.py)；exposure=营收归因份额∈[0,1]、segment 太粗→confidence='low' 且不编造数字；只接受 themes.yaml 合法 theme key、越界/未知项丢弃；backend=claude -p(不接 API key)→这是 operator 步骤、绝不进 nightly CI；候选写 candidates/(gitignore 的中间产物,可重跑)",
        "verification": "make theme-extract TICKER=NVDA 产 candidates/NVDA.json，≥1 候选且每项 theme∈themes.yaml、exposure∈[0,1]、confidence∈{high,medium,low}；非法 key/越界 exposure 被丢弃并告警"
      }
    },
    {
      "notes": "",
      "path": "themes/land.py",
      "kind": "script",
      "module": "themes",
      "stage": "implemented",
      "contract": {
        "purpose": "M4.5 落库：themes/approved/*.json → theme_membership upsert(每轮 pipeline 重播)",
        "invariants": "approved 文件是 human membership 的 durable SoT(DB 夜间从头重建)；make themes 内在 seed.py 之后跑→PIT 解析(db.theme_membership_asof)处 approved 晚 as_of 胜过 seed 基线(C3)；校验 theme key∈themes.yaml + exposure∈[0,1] + approved_by 非空(C6)，违则硬报错；幂等(INSERT OR REPLACE)",
        "verification": "make themes 后 theme_membership 含 approved 行(source=llm、approved_by 人名)；land --dry-run 列行不写库；坏 key/越界 exposure/空 approver 硬报错 exit"
      }
    },
    {
      "notes": "",
      "path": "themes/review.py",
      "kind": "script",
      "module": "themes",
      "stage": "implemented",
      "contract": {
        "purpose": "M4.5 human-in-loop 审批：candidates → themes/approved/<TICKER>.json(人审后的 durable SoT)",
        "invariants": "human 是权威(C6)：审批可自由覆盖 LLM exposure、删候选、补 LLM 漏的主题；--by<approver> 必填(具体人名非 bot)、缺则报错；as_of_date 默认=candidate 的 10-K filing 日(PIT 信息可得日，非审批操作日：否则 today 日审批在最新 EOD board[as_of=前一交易日]要等下个 session 才解析)、可 --as-of 覆盖(C3 point-in-time，再审写新状态、绝不原地改史)；approved 文件自含 provenance(model/filing/per-row llm_exposure)；写 git 持久而非 DB(夜间重建)",
        "verification": "review --approve \"AI=0.9,SEMI=1.0\" --by sejonep 产 approved/NVDA.json，rows 含 exposure+approved_by+llm_exposure、as_of=filing 日；board PIT(≥filing 日)即解析到 approved；缺 --by 报错 exit；无 --approve 仅展示候选不写盘"
      }
    },
    {
      "notes": "",
      "path": "themes/run.py",
      "kind": "script",
      "module": "themes",
      "stage": "implemented",
      "contract": {
        "purpose": "M4.5 编排：extract → review → land 三步单入口，per ticker",
        "invariants": "review 保持交互 BY DESIGN(C6：人必须先看候选才能入库)→无 --approve 时只 extract+展示、绝不自动批准；--approve+--by 才 forward review+land；薄 glue、不复制三步逻辑",
        "verification": "run --ticker NVDA 跑 extract+展示候选；run --ticker NVDA --approve ... --by ... 跑 review+land；--skip-extract 复用现有候选文件"
      }
    },
    {
      "notes": "",
      "path": "themes/seed.py",
      "kind": "script",
      "module": "themes",
      "stage": "implemented",
      "contract": {
        "purpose": "从 ingest/universe_seed.txt 的主题分组结构化出 bootstrap theme_membership(point-in-time 基线快照)：按 themes.yaml 的 seed_header 数据驱动把段头映射到 theme,写 exposure 初值 + 单一 baseline as_of + source=seed + approved_by=seed,经 compute/db.upsert_theme_membership 落库",
        "invariants": "是 LLM/EDGAR 营收锚定(M4.5)前的确定性兜底、粗初值非权威：exposure=SEED_EXPOSURE 平值、approved_by='seed'(未经 human 审批的哨兵,C6)、baseline as_of 默认回填到 daily_bars 最早日(baseline_as_of：theme index 从首个 as_of 前向构建，seed 作为长期粗基线锚到最早价格 bar→index 覆盖完整历史、周线 RS-Ratio 才产得出；空库回退 SEED_AS_OF；PIT 不破坏——M4.5 approved 在更晚 as_of 仍前向胜出)；映射纯数据驱动(seed_header)无代码硬编码观点,reset-on-comment 解析令未识别段头下的 ticker 显式 unmapped 而非静默误配；多对多由数据天然支持(同票列两段→两行),当前 seed 每票一段、真实 overlap(NVDA∈AI+SEMI)按设计留 M4.5 营收锚定;幂等(clear 后全量重写,source=seed 完全派生);依赖 PyYAML(产品代码,engine/index.py 仍 stdlib-only)",
        "verification": "python3 themes/seed.py --dry-run 报 8 theme、48 票全映射零 unmapped、as_of=daily_bars 最早日(非硬编码近期日)；--db 写库后 db.theme_keys()=8、代表票各归位(NVDA∈AI / AMD∈SEMI / CRWD∈CYBR / RKLB∈SPACE / SNOW∈CLOUD / ISRG∈ROBO / COHR∈OPTIC / CEG∈NUKE)；全行 source=seed、approved_by=seed、exposure=0.6"
      }
    },
    {
      "notes": "",
      "path": "themes/themes.yaml",
      "kind": "config",
      "module": "themes",
      "stage": "implemented",
      "contract": {
        "purpose": "8 个 concept theme 定义的单一真源：key/name/color_var(--th-*)/cap/seed_header/description/keywords。被 themes/seed.py(据 seed_header 映射 universe_seed 分组 + 校验 key)、compute/theme_index.py(cap 限权,M4.2)、themes/extract.py(keywords 喂 LLM 候选,M4.5)、web theme.css/THEME_VAR(镜像 color_var→hex)消费",
        "invariants": "8 theme = AI/ROBO/SPACE/OPTIC/SEMI/NUKE/CYBR/CLOUD(PRD §8.2 / 附录 C)；color_var 对齐 web theme.css 的 --th-*(本文件为权威,web 镜像)；cap=theme index 单票权重上限(非市值加权 C4,M4.2 消费)、当前 0.30、按真实成员 just-in-time 调(不回测 alpha 买 robustness)；seed_header 精确匹配 universe_seed.txt 段头(数据驱动映射,改段头须同步)；只定义 theme 不分配 ticker(membership 在 theme_membership 表,point-in-time)；多对多绝不 MECE",
        "verification": "yaml.safe_load 得 themes 列表 8 条、每条含 key/name/color_var/cap/seed_header/description/keywords；color_var 均见 web/src/theme.css；themes/seed.py 据 seed_header 把 universe_seed.txt 48 票映射到 8 theme、零 unmapped"
      }
    },
    {
      "notes": "",
      "path": "web/README.md",
      "kind": "doc",
      "module": "web",
      "stage": "planned",
      "contract": {
        "purpose": "静态客户端模块规划：canvas + duckdb-wasm 渲染五个 surface",
        "invariants": "UX 合同 = equity-monitor-v2.jsx；trail 仅 pinned 且 Ocean 无 autoplay；所有 surface 数据从单一引擎派生",
        "verification": "五 surface 行为与 PRD §9 一致；全局原语（旋钮/scope）符合 §9.1"
      }
    },
    {
      "notes": "",
      "path": "web/index.html",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Vite 入口 HTML：挂载点 #root + 加载 src/main.tsx",
        "invariants": "暗色 color-scheme；单一 #root 挂载；模块脚本入口 /src/main.tsx；charset/viewport/title 就位",
        "verification": "vite build 产出 dist/index.html 含打包后脚本/样式引用"
      }
    },
    {
      "notes": "",
      "path": "web/package-lock.json",
      "kind": "config",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "锁定 web 依赖精确版本树（npm 生成）以复现构建",
        "invariants": "与 package.json 同步、不手改；入库供 npm ci 还原确定的 node_modules（web/node_modules 走 gitignore）",
        "verification": "npm ci 据此还原；npm install 不再产生 lockfile diff"
      }
    },
    {
      "notes": "",
      "path": "web/package.json",
      "kind": "config",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "web client 依赖与脚本清单（Vite + React + TS + Vitest）：dev / build / preview / typecheck / test",
        "invariants": "type=module(ESM)；React 19 + Vite 6 + TS5 + Vitest 2；build = tsc 类型检查 + vite 产 dist；test = vitest run(C9 parity)；不引后端/运行时网络依赖",
        "verification": "npm install 成功；npm run build 通过；npm test 全绿"
      }
    },
    {
      "notes": "",
      "path": "web/src/App.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "应用 shell：固定顶部控制栏（5 tab + early⟷reliable 旋钮 + 权重条 + scope chip）+ 中央 surface 面板 + 全局 scope/pinned 单一真源",
        "invariants": "5 surface 固定顺序、默认 Discovery；旋钮 k 驱动权重条 + 透传 Discovery 实时重排；discovery 渲染 <Discovery k scope pinned limit=DISCOVERY_LIMIT(20)>（board 上限 top-20，bounded/decide §9.3）、ocean 渲染 <Ocean scope setScope pinned setPinned>(lasso writer)、其余占位；scope(all|sector|theme|pinned) + pinned 集 = App 单一真源、跨 tab 粘滞、scope chip 可一键清(pinned 显计数)(C8/C10)；权重曲线来自 composite.ts(C9)；rotation tab 接 <Rotation>(传 scope/setScope/onJumpTab=setTab/k)、Rotation 为第 2 个 scope writer(league 行/线→sector)；header as_of badge(D.4)：useEffect load manifest → 显 as_of + 数据龄 + 陈旧色(fresh/aging/stale dot)，陈旧/缺数据可见、绝不静默当新鲜；Valuation/Stock 视图已挂载(M5 预览)、placeholder 分支移除；selectedTicker 状态 + openStock 路由让任意 surface(Discovery 卡 / Valuation 行)的 onOpen 切到 Stock per-name",
        "verification": "npm run build 通过；SSR app chrome 含 wbars/wbfill + RELIABLE/EARLY；旋钮改 k → Discovery 重排；ocean tab 渲染 Ocean canvas；scope≠all 显 chip + 可清"
      }
    },
    {
      "notes": "",
      "path": "web/src/components/ChartCursor.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "时间游标的共享 SVG 读数 chip（Discovery MiniChart / Rotation / Stock stack）：贴游标右侧、近右缘自动翻到左侧防裁切",
        "invariants": "仅当图有 live hoverIndex 时渲染，故 SSR(renderToStaticMarkup)永不出现、不动现有 markup 断言；mono 字宽估算定 chip 宽(无 DOM 测量、SSR 安全)；出界翻转由 viewW 判定；color 默认 --txt、可传分量/线色；用 .chcur(pointer-events:none)/.chcur-bg/.chcur-txt 类",
        "verification": "被 MiniChart/StockStack/Rotation 引用；三图 SSR 测试静息态不含游标 chip；npm test 全过"
      }
    },
    {
      "notes": "",
      "path": "web/src/components/EvidenceCard.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "evidence-card = Stock 的 collapsed 态（PRD §9.1.3 + §10.8）：头部 + 🔥持续点火标 + d/d + 可展开 composite 角标 + 点火证据 strip + theme tags + MiniChart + 6 原始字段",
        "invariants": "证据优先、永不给 buy/target；点火榜上 composite 降为「已确认」副读角标（badge 显外部传入 live score 按当前 k 重算、≥62/≥47 三档色、展开露 5 分量 c_*+权重随 k 变无黑箱），candidate 时显 🔥+ign_persist_days 标；点火证据 strip 取 stock.ignition.evidence 同源 derived_daily(C9 不重算)：突破日 breakout_day(MM-DD)+days_since_breakout、放量× vol_mult(=ig_vsurge 5/60 比，区别于 6 字段里的 50d vol)、步速比 step_rate_ratio(=ig_accel 可读形，显示 clamp ±20× 防 ret50≈0 爆值)、收复 MA50(reclaimed_ma50 即 ig_breakout gate)；d/d=引擎默认权重日变；点 badge 仅展开、点卡其他处 onOpen(Stock)",
        "verification": "Discovery.test SSR 含 ticker/live score badge/d-d 箭头/6 字段/展开 5 分量+权重%/无黑箱/点火证据 strip(brk·vol·step·MA50)/🔥candidate 标/step_rate clamp(>20× 且原始爆值不 verbatim)/reclaimed 收复；build 通过"
      }
    },
    {
      "notes": "",
      "path": "web/src/components/MiniChart.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "纯 SVG mini-chart：~90d K线 + MA50/150/200 + 成交量 + 52w 高虚线 + 常驻 MM/DD 日期轴 + hover 时间游标读收盘价（PRD §9.3）",
        "invariants": "props-only 无 DOM 测量（SSR 与浏览器渲染一致、确定）；K线涨绿跌红；价轴含 low/high/MA/52w-high 使虚线在视野；MA 在 null 处断笔；底部常驻日期刻度(MM/DD，起/中/止)；hover 时间游标=竖线+收盘价 marker+date·close 读数，为 hover-only overlay(hoverIndex 静息 null)故 SSR 不渲染游标；几何转写自 UX 合同",
        "verification": "SSR renderToStaticMarkup 含 <svg>/<rect>/MA stroke var/stroke-dasharray、静息态无游标；Discovery.test 10 项过；npm run build 通过"
      }
    },
    {
      "notes": "",
      "path": "web/src/components/StockStack.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "M5.4 Stock 四格时间轴对齐 stack（PRD §9.6）：PRICE/VOLUME/REVENUE/P-S 四格共用一条 x 轴 + 季度网格贯穿 + 常驻 MM/DD 日期轴 + 贯穿四格的 hover 时间游标的纯 SVG",
        "invariants": "props-only 纯 SVG、SSR 同形；主 x 轴=price.dates(~2y 日频)，所有格共用 X(i)；季度网格=每 revenue period_end 映射到最近交易日 index 的竖线贯穿四格；PRICE=K线+MA50/150/200+52wh 虚线、VOLUME=量柱(涨绿跌红)、REVENUE=季度 TTM bars(YoY 增绿降红、无同比灰)、P/S=每日线；各格独立 y range；price/ps date→index 直查、revenue period_end→最近≤index；底部常驻日期刻度(MM/DD，~6 点)；hover 时间游标=一条竖线贯穿四格 + 各格读该日各自序列(PRICE close/VOL 量/REVENUE 该日≤period_end 的最近季 TTM/P-S 当日值)+读数，为 hover-only overlay(hoverIndex 静息 null)故 SSR 不渲染游标；空 price 安全(返回空 svg)",
        "verification": "StockStack.test SSR：四格标签 PRICE/VOL/REVENUE/P-S、季度网格、PS 蓝线、candles/bars rect、空 price 不崩、静息态无游标；浏览器 smoke 四格对齐+季度竖线贯穿+游标各格读数"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/__fixtures__/board.sample.json",
      "kind": "config",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "C9 单测 + AC-M1/AC-M7 渲染测试用样本 board.json（export/board.py 真实产物，30 票，含 ignition 块）",
        "invariants": "export/board.py 产物、含 components c_* + 导出 composite + knob_default_k + 每票 ignition 块(ignition/ign_pct/ign_persist_days/candidate/components{}/evidence{})；≥18 票满足 AC-M1「≥18 卡」、≥1 票 candidate=true 供 AC-M7 持续点火排序断言、含 step_rate_ratio>20 票供 clamp 断言；固定供回归对拍；非运行时数据(运行时走 public/data、gitignore)",
        "verification": "composite.test.ts + Discovery.test.tsx 据此对拍/渲染；fixture 与 fresh export byte-identical(同 seed 确定可复现)"
      }
    },
    {
      "notes": "派生自 export/ocean.py(M2.1)；与 board.sample.json 同为提交版测试 fixture",
      "path": "web/src/lib/__fixtures__/ocean.sample.json",
      "kind": "config",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "AC-M2.2 Ocean 渲染测试用样本 ocean.json（export/ocean.py 真实产物，520 票 fixture→517 票×14 周）",
        "invariants": "由真实 export/ocean.py 在合成 fixture DB(seed 7,520 票)产出、非手写；每票 pts 与 weeks 对齐、最新周非空(≥500 可渲染点)；rs/val∈[0,100]、val 底=便宜；提交版固定快照(__fixtures__/ 不被 gitignore，区别于派生的 data/ocean.json)",
        "verification": "Ocean.test.tsx import 后断言 count≥500、最新周全非空、每票 sector 可识别；再生命令见 Ocean.test.tsx notes"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/__fixtures__/rotation.sample.json",
      "kind": "config",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "AC-M3 Rotation 渲染测试用样本 rotation.json（export/rotation.py 真实产物，110 票 fixture→11 sector×52 周）",
        "invariants": "export/rotation.py 对 fixture(seed 7、110 票)的真实产物、非手写；含 11 sector RS-Ratio 周序列 + 4 态 league(breadth/at-high/composite 中位/agg EV-S/rel return) + members ticker；供 M3.4 web SSR 测试(react-dom/server 断言 path 数=bucket 数、4 态标签、钻进 N=1)；fixture 确定性 → 提交版字节稳定",
        "verification": "JSON.parse 通过；buckets=11、weeks=52、4 态齐、各 bucket rs_ratio 长度=weeks；与 export/rotation.py 当前产物一致"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/__fixtures__/rotation.theme.sample.json",
      "kind": "config",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "AC-M4 theme Rotation 渲染测试用样本 rotation.theme.json（export/rotation.py --bucket-type theme 真实产物，offline fixture→4 theme×39 周）",
        "invariants": "export/rotation.py --bucket-type theme 对 fixture 的真实产物、非手写；含 4 theme RS-Ratio 周序列(theme index point-in-time、非市值加权 exposure+cap)+ 4 态 league(成员取自 theme_membership PIT)+ members ticker；bucket_type='theme'、无 etf(null)；供 Rotation theme 模式 SSR 测(path 数=4、THEME_VAR --th-* 上色、theme drill、非市值加权标注)；fixture 确定性 → 提交版字节稳定；weeks 因 theme index 从首个 as_of 起可少于 sector(52)",
        "verification": "JSON.parse 通过；bucket_type=theme、count=4、各 bucket rs_ratio 长度=weeks、末值非空；与 export/rotation.py --bucket-type theme 当前产物一致"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/__fixtures__/stock.sample.json",
      "kind": "config",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "M5.4 Stock SSR 测试 fixture：从 stock_bundle.py 导出的一个 bundle(TT07)，注入 Stock/StockStack 跳过 fetch",
        "invariants": "镜像 stock/<T>.json bundle(meta/components/valuation/price/revenue_q/ps_series)；含 ROBO theme chip(exposure 0.58)验 chip%；price/revenue/ps 序列非空覆盖四格渲染",
        "verification": "Stock.test/StockStack.test 注入渲染；header/卡/分量/四格齐"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/__fixtures__/valuation.sample.json",
      "kind": "config",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "M5.2 Valuation SSR 测试 fixture：从 valuation.parquet 转出的 rows，注入 Valuation 跳过 duckdb-wasm",
        "invariants": "逐字段镜像 valuation.parquet（含 peg/margin/themes/freshness/as_of_*）；覆盖 fresh/stale/overdue + 多 theme 票（TT01='AI,SEMI'）；date 列 ISO 字符串（与 duckdb view strftime 输出一致，保证注入路径与浏览器路径同形）",
        "verification": "Valuation.test 注入 data.rows SSR 渲染；列/freshness/themes 齐、可复跑"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/chart-hover.test.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "chart-hover 纯几何单测门：坐标换算 + band/point index 反推 + 最近线 + 轴刻度 + 日期格式",
        "invariants": "无 DOM(组件喂 getBoundingClientRect 数值)、纯断言；覆盖 viewBoxXFromClient 缩放与零宽防御、bandIndexAt floor+半格容差+退化 null、pointIndexAt round+n≤1、nearestSeriesAt 跳 null+全 null 返 -1、axisTickIndices 两端均布+小 n 退化、tickDate MM/DD+非 ISO 透传",
        "verification": "npx vitest run src/lib/chart-hover.test.ts 13 项全过"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/chart-hover.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "图表 hover 时间游标的共享纯几何（Discovery MiniChart / Rotation / Stock stack）：鼠标位置→最近数据 index + 常驻日期轴刻度布局",
        "invariants": "无 DOM/无 React、纯函数可头测；游标是 hover-only overlay(hoverIndex 静息=null)故 SSR/初始不渲染游标、不动 renderToStaticMarkup 断言；viewBoxX/YFromClient 经 getBoundingClientRect 比例把 clientXY 换算到 viewBox 坐标(图按 width=100% 渲染)；bandIndexAt=等距 band 布局(点心 PL+i·bw+bw/2，MiniChart/StockStack)、pointIndexAt=端点对齐布局(padL+i/(n-1)·跨度，Rotation)，区外含半格容差返回 null；nearestSeriesAt=按 viewBox-y 最近选 series(跳 null)；axisTickIndices=两端均布刻度 index、tickDate=ISO→MM/DD 防御式",
        "verification": "chart-hover.test.ts(13 项)断言坐标换算 + band/point 反推 + 边界 null + 最近线 + 刻度 index + 日期格式；npm test 全过"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/composite.test.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "C9 parity 单测(vitest)：前端 composite 移植 vs 引擎导出对拍",
        "invariants": "weights(k) 命中公式(0.20+0.03k…)与 Σ=1；k clamp；对 board.sample.json 每票 recompute@knob_default_k 与导出 composite 漂移<0.02",
        "verification": "npm test(vitest run)全绿"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/composite.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "C9 前端 composite 重算：移植 compute/signals.py 的 weights(k) 系数 + 100·Σwᵢ·cᵢ",
        "invariants": "weights(k) 系数与 compute/signals.py 逐位一致(C9)、k clamp[0,1]、Σ=1 全 k；composite 用导出的 c_* 重算、绝不在前端 re-derive 引擎；前端唯一权重曲线来源",
        "verification": "composite.test.ts(vitest)对拍：weights 曲线 + Σ=1 + recompute@knob_default_k 复现导出 composite(<0.02)；Python signals.weights 交叉对照逐位相等"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/data.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "载入 nightly 静态快照：board.json(Discovery) + ocean.json(Ocean) + rotation.json/rotation.theme.json(Rotation sector/theme)（export/*.py 产物）",
        "invariants": "loadBoard/loadOcean 同范式：fetch 路径用 import.meta.env.BASE_URL 尊重 Vite base='./'；非 2xx 抛错供上层降级；分别返回 BoardData/OceanData(types.ts 镜像)；支持 AbortSignal 取消；loadRotation(signal,bucketType='sector') 按 bucketType 取 rotation.json(sector)或 rotation.theme.json(theme)、同 BASE_URL fetch 范式(两文件因 theme index 从首个 as_of 起 weeks 轴不同)；loadManifest 载入 manifest.json(新鲜度 D.4，同 BASE_URL fetch 范式)；loadStockBundle/loadStockIndex 懒加载 per-name bundle(M5.4)",
        "verification": "Discovery 按 loadBoard、Ocean 按 loadOcean、Rotation 按 loadRotation(sector/theme) 运行时载入渲染；对应 json 缺失时上层显 NO DATA 态"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/duckdb.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "M5.2 duckdb-wasm 加载层：浏览器读 valuation.parquet + SQL 做 metric 排序（PRD §9.5 在浏览器查 Parquet）",
        "invariants": "wasm+worker 从 URL 加载、绝不入 dist(wasm 35-41MB 超 Pages 25MiB 限制)：生产 VITE_DUCKDB_R2_BASE 指 Cloudflare R2(用户自有存储非第三方 CDN，脊柱:parquet 仍自托管、仅静态 wasm lib 外移)、dev 无 env 时 fallback jsDelivr;跨域 worker 用 same-origin blob importScripts 包一层;AsyncDuckDB 实例 + 注册 parquet 单例复用、selectBundle 自动选 mvp/eh；buildValuationSql 纯函数（cheap-asc 升序/quality-desc 降序 + NULLS LAST + ticker tiebreak）导出供测；CREATE VIEW 用 strftime 把 DATE→ISO 字符串（避 Arrow→JSON 的 epoch-millis）；duckdb 只做读 parquet + metric 排序，scope filter/common-vintage pctile/三档色留 Valuation.tsx（theme scope 非 SQL 列、pctile 是 cohort 统计）",
        "verification": "buildValuationSql 单测（ORDER BY metric ASC/DESC + NULLS LAST, ticker）；浏览器 smoke：Valuation tab duckdb-wasm 查 parquet 渲染全 universe 行 + PEG/Mgn% + 三档色 + ISO 日期（非毫秒）"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/format.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "board 驱动视图(Valuation/Stock)的共享展示格式化：fmtMktcap/num/pct/scoreColor + ignition 点火证据格式化(fmtStepRate/SR_CLAMP/fmtMonthDay)",
        "invariants": "纯函数无 DOM、SSR 可测；null→em-dash；scoreColor 按附录C(≥62 hi/≥47 mid/else lo)；fmtStepRate 把步速比(ig_accel 可读形)显示 clamp 在 ±SR_CLAMP(=20×、display-only，引擎 ranked ig_accel 不受影响)、超界前缀 >/<，与 EvidenceCard(M7.3)本地副本同阈值同语义(共享主源，Stock 点火诊断复用此处而非另起一套)；fmtMonthDay 取 ISO 日期 MM-DD；EvidenceCard(M1/M7.3)仍保留本地副本、不动已验证代码(后续统一重构)",
        "verification": "Valuation/Stock 视图与其测试消费；Stock.test 断言 fmtStepRate clamp(>20× 显 &gt;20×)；tsc 通过"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/freshness.test.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "freshness 纯函数单测(D.4)：dataAgeDays/freshness/ageLabel 边界",
        "invariants": "覆盖 UTC 归一、阈值边界(3/7)、NaN/负→stale、ageLabel 各态；vitest run",
        "verification": "npx vitest run：freshness.test 全过、不破坏既有"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/freshness.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "as_of badge 数据新鲜度纯函数(D.4)：dataAgeDays(as_of→today 日历天) + freshness(fresh≤3/aging≤7/stale) + ageLabel",
        "invariants": "纯函数无 DOM(SSR 可测)；UTC 归一(时刻/DST 不影响天数)；阈值容忍周末(周五数据周一 3 天=fresh)；NaN/负(未来 as_of)→stale(可疑、绝不静默当 fresh)",
        "verification": "freshness.test.ts：dataAgeDays 日历天/NaN、freshness 阈值边界(3/7)、ageLabel today/N天前/陈旧"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/ocean-draw.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Ocean canvas 纯绘制库：固定 RS×Val 平面的几何/配色/尺寸 helpers + drawOcean 单次绘制一周快照(含 pinned trail/arrow + lasso rect) + nearestPoint/pointsInRect 命中测试（PRD §9.2）",
        "invariants": "无 React/无真实 canvas 依赖、纯函数可头测；domain 0-100→像素、sy 反转故 val=0 在底=便宜(§9.2)；size=clamp(2+√(mktcap$B)·0.34,1.6,11)；颜色三模式返回 CSS 变量名(不写裸 hex，theme.css 为 hex SoT)，resolvePalette 运行时解析、测试注入假调色板；quadrant 强(rs≥50)+便宜(val<50)=lead/强+贵=weak/弱+便宜=improving/弱+贵=dim；inScope 按 scope 判定(pinned=ticker∈pinned 集)；drawOcean 画象限 tint+(50,50)十字线+当周点(in-scope/主题成员全径、否则淡至 1.2px C10)+可选 hover 白环+pinned 装饰(彩色 trail 断 null 段+当前位箭头+ticker 标签)仅当 pinned 数≤TRAIL_CAP(8) 时整画、超过则每只仅留强调点(无 trail/箭头/label)由 scope 淡化承载选择(C2 认知带宽守卫，防 lasso 大区域 trail spaghetti)+lasso 选框，并返回非淡点屏幕位置；nearestPoint 线性扫描 ~r+5px 最近点(NFR-8)；pointsInRect 返回框内 ticker(支持反向拖拽)；CanvasLike 为 CanvasRenderingContext2D 结构子集",
        "verification": "Ocean.test.tsx：radiusFor/makeScales(底=便宜)/quadrantVar/colorVar/withAlpha/nearestPoint/pointsInRect/inScope(pinned) 纯测；drawOcean mock-ctx 在 517 票 fixture 画 ≥500 arc 落框内 + 静态层 + scope 淡化 + hover ring + pinned trail(多 stroke + ticker fillText) + 大 pinned(>TRAIL_CAP) 不画 N 条 trail(text 空、仅强调点) + lasso rect(多 fillRect+stroke) + 切周点位移动"
      }
    },
    {
      "notes": "",
      "path": "web/src/lib/rotation-draw.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Rotation 多线 + N=1 SVG 几何纯函数（无 DOM，可 SSR 测）：scale/path/末值排序标签/N=1 短窗斜率分段；颜色复用 ocean-draw SECTOR_VAR + THEME_VAR(附录 C)",
        "invariants": "纯函数无副作用无 DOM；MULTI/SOLO geom 取自 UX 合同 viewBox(jsx RSRatioLines/SoloRSLine)；multiScale 夹 100(lo≤98.5/hi≥101.5)、soloScale(lo≤99/hi≥101)；linePath 遇 null 断线不造段；endLabels 末值排序+gap 防重叠；soloSegments 短窗 K=3 斜率 up/down；bucketColorVar 返回 CSS-var 名(SVG 直接 var()，不 resolve hex)——sector 名→SECTOR_VAR、theme key→THEME_VAR(名不碰撞)、否则 --dim2",
        "verification": "Rotation.test.tsx：multiScale 夹 100、linePath null 断线(2 个 M)、soloSegments 单调全 up、endLabels gap≥13；theme 线用 --th-* 色"
      }
    },
    {
      "notes": "",
      "path": "web/src/main.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "React 根渲染入口：StrictMode 挂载 App + 注入全局 theme.css",
        "invariants": "createRoot 挂到 #root；引入 theme.css（配色 SoT）；仅做挂载、不含业务逻辑",
        "verification": "构建后页面挂载 App；shell 渲染无运行时控制台错误"
      }
    },
    {
      "notes": "",
      "path": "web/src/theme.css",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "暗色主题与配色 SoT：附录C 全调色板 CSS 变量 + shell 组件类 + evidence-card/Discovery/Ocean 类 + 共享图表游标/日期轴类",
        "invariants": "暗色唯一(--bg #080b11)；hex 取自 PRD 附录C / UX 合同、组件与 canvas 只引 var()/var 名不写裸 hex；选中 tab 高亮绿 --accent；evidence-card(.ecard/.ec-*/.ecgrid)、权重条(.wbars/.wb/.wbfill)、d-d(.ec-headr/.ec-dd)、Ocean(.ocean/.oc-ctrl/.ocmode/.ocwrap/.occanvas/.oax-*/.oquad/.ocscrub/.ocrange/.ocpins/.otip*/.otrow/.otags/.ohint) 转写自 UX 合同；加 Rotation 组件类(.rot/.rleague/.rlrow/.rot-summary/.rrgsvg/.seg/.qchip 等，复用现有调色板 var)；D.4 freshness badge 类(.dot.fresh/aging/stale + .asof2.fresh-aging/stale)复用 grn/amb/red；新增 .valn-*(横截面表 + as-of 三档 vfr-fresh/stale/overdue + vint)与 .stk-*(per-name 面板 + theme chip exposure + 6 估值卡 + 5 分量条)(M5 预览)；.stk-stackwrap/.stk-stack(M5.4 四格 stack 容器)；新增共享图表 hover 游标 + 常驻日期轴类(.chax/.chcur/.chcur-line/.chcur-dot/.chcur-bg/.chcur-txt)，三图(MiniChart/Rotation/Stock)复用、.chcur 设 pointer-events:none 使游标层不吃 svg 自身 mousemove；新增持续点火类(M7.3 PRD §10.8)：.ec-ign(🔥candidate 标，绿 color-mix 底)、.ec-ignev(点火证据 strip flex-wrap)+.ec-ignev-i/em/b/-d(brk·vol·step·MA50 读数，复用 grn/dim/txt/panel var)；M7.4 加 Stock 点火诊断面板类(.stk-ign 琥珀色调与 composite 蓝区分/.stk-ignt*/.stk-ignhead/.stk-ignflag(+.on 绿)/.stk-ignmeta/.stk-igntl*(persistence 日格 timeline、.on=lit 绿)/.stk-ignev(复用 .ec-ignev 仅调边距)/.stk-igncomp*/.stk-igncrow/.stk-igncl/.stk-igncw，复用现有 grn/amb/dim/txt/line var)",
        "verification": "构建后 CSS 含附录C 关键 hex 与 .ec-*/.ec-ign*/.stk-ign*/.wb*/.oc*/.otip/.chcur/.chax 类；视觉对 UX 合同"
      }
    },
    {
      "notes": "",
      "path": "web/src/types.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "board.json(M1.1/M7.2) + ocean.json(M2.1) + rotation.json(M3.3) + stock bundle(M5.3/M7.4) 的 TS 类型镜像 + SurfaceId/Scope，前端数据桥",
        "invariants": "结构与 export/board.py、export/ocean.py、export/stock_bundle.py 输出逐字段对齐；引擎可空处置 null；BoardData.components=5 分量供前端按 k 重算(C9)、绝不在前端重算引擎；Stock(card).ignition 与 StockBundle.ignition(均可选/可空，pre-M7 fixture 或引擎无值时缺) = 第二引擎(PRD §10.8)：Ignition{ignition/ign_pct/ign_persist_days/candidate + IgnitionComponents{accel,expand,vsurge,breakout,rsturn}(原始 self-relative 非 [0,1]) + IgnitionEvidence{breakout_day,days_since_breakout,vol_mult(=ig_vsurge 5/60 比≠Evidence.vol_mult 50d),step_rate_ratio(ret50≈0 会爆需前端 clamp),reclaimed_ma50,ma50}}，StockBundle 与 board card 共用同一 Ignition 类型(同源 C9)；BoardData 顶层 ignition_coverage/candidates/persist_min/recon_max_drift(可选)；OceanData/OceanStock/OceanPt：pts 与 weeks 对齐、pt 可空、rs/val∈[0,100] val 底=便宜；RotationData/RotationBucket/RotationState 镜像 rotation.json(4 态枚举)；ManifestData 镜像 manifest.json；ValuationRow/ValuationData 镜像 valuation.parquet(M5.2)；StockBundle/RevenueQuarter/PsPoint/StockIndex 镜像 stock bundle(M5.3/M5.4)",
        "verification": "tsc 通过；Discovery 按 BoardData、Ocean 按 OceanData、Stock 按 StockBundle(含 ignition)消费无类型错"
      }
    },
    {
      "notes": "",
      "path": "web/src/views/Discovery.test.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "AC-M1 + AC-M7 + AC-M2.4 渲染 gate（vitest）：SSR 渲染 Discovery/EvidenceCard 断言里程碑可见契约 + 持续点火排序 + 点火证据 + 全局 scope 过滤",
        "invariants": "renderToStaticMarkup 断言：AC-M1 ≥18 卡 / 6 字段 / 展开 5 分量+权重+无黑箱 / d-d 箭头 / mini chart svg+MA+52w；AC-M7 持续点火排序=candidate→ign_persist_days desc→ign_pct desc(candidate 浮顶、高 persist 先、整体序≠纯 composite 序)、旋钮 k=0 vs k=1 排序不变(只属 composite 不动点火榜)、composite 仍随 k 重算作副读、点火证据 strip(brk/vol/step/MA50)+🔥candidate 标+step_rate clamp(>20× 原始爆值不 verbatim)+收复 MA50；AC-M2.4 scope=sector|pinned 过滤、scope=all 全显(C10)；无浏览器、用 board.sample.json fixture(含 ignition 块)",
        "verification": "npm test / make web-test 全绿（本文件 19 用例）"
      }
    },
    {
      "notes": "",
      "path": "web/src/views/Discovery.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Discovery 持续点火榜（PRD §9.3 + §10.8）：2 列 evidence-card 网格、respect 全局 scope 后按持续点火排序（非 composite）",
        "invariants": "先按全局 scope filter(all/sector/theme/pinned 由 App 透传，§9.3 先 filter 再排序 C10)再按持续点火排序 byIgnition：candidate(=ign_pct top-decile 且 persist≥persist_min)优先 → ign_persist_days desc → ign_pct desc(瞬时点火无 lift、persistence 主导)；旋钮 k 只属 composite(PRD P7)不动点火排序——k 改仅重算每卡 composite 副读 badge(C9、绝不重算引擎)；卡片 weights/score 仍用 composite.ts 在当前 k 算作「已确认」副读；ignition 数据从 board.json stock.ignition 读(C9 同源不前端重算)；loading/error/empty 三态；initial 注入便于 SSR/测试；可选 limit prop slice top-N(Discovery 本体由 App 传 20 设上限；Rotation 钻进成员预览传更小 N、scope=sector)；脚注示点火候选数",
        "verification": "SSR：持续点火排序(candidate 浮顶/persist desc/序≠composite 序)、k=0 与 k=1 排序不变(旋钮不重排点火榜)、composite 副读随 k 重算、≥18 卡；scope=sector/pinned 仅留对应卡、scope=all 全显；build 通过"
      }
    },
    {
      "notes": "ocean.sample.json 再生：make fixture-pipeline FIXTURE_ARGS=\"--tickers 520 --seed 7\" && python export/ocean.py --out web/src/lib/__fixtures__/ocean.sample.json",
      "path": "web/src/views/Ocean.test.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "AC-M2.2/2.3/2.4 提交版回归 gate（vitest）：Ocean 纯绘制库 + drawOcean mock-ctx(≥500 点/scrubber/hover/pin trail/lasso/scope 淡化) + SSR scaffold",
        "invariants": "无浏览器：纯 helpers(radiusFor/makeScales/quadrantVar/colorVar/withAlpha/nearestPoint/pointsInRect/inScope) 直测；drawOcean recording mock-ctx 计 arc≥500 落框内、scope 淡化、hover ring、切周点位移动、pinned trail(多 stroke+ticker fillText)、scope=pinned 仅 pinned 非淡、大 pinned(>TRAIL_CAP) 不画 N 条 trail label(text 空、由淡化承载)、lasso rect(多 fillRect+stroke)；Tip/Ocean SSR 断言；fixture=ocean.sample.json",
        "verification": "npm test / make web-test 全绿（本文件 23 用例）"
      }
    },
    {
      "notes": "themes 数据 M4 前空→theme 配色模式暂全淡、theme 选择器留 M4；Makefile export 接线 + 浏览器性能验证留 M2.5",
      "path": "web/src/views/Ocean.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Ocean surface（PRD §9.2）：固定 RS×Val canvas 散点 + 配色切换 + √市值尺寸 + 手动 WEEK scrubber + hover tip + click pin(trail) + lasso 框选 set 全局 scope(首个 scope writer C10)",
        "invariants": "读 ocean.json(loadOcean) 或 initial 注入；canvas 2x retina；useEffect 调 drawOcean、色板 resolvePalette、绘制点位存 pos ref；WEEK scrubber 手动 range(无 autoplay C2)、未拖跟随最新周；hover 经 nearestPoint+Tip overlay(7 字段 §9.2)；mousedown/move/up 区分 click(<4px→toggle pin) 与 drag(→pointsInRect 选中 setPinned + setScope({pinned}))；trail/箭头仅 pinned(C2)；pinned 与 scope 由 App 单一真源透传、Ocean 仅作 writer；respect scope 淡化非成员(in0.72/out0.06 C10)；错误/加载态降级",
        "verification": "SSR 含 canvas+三配色切换+轴标+WEEK n/N+手动周 range+pinned 计数；Tip SSR 7 字段；命中/绘制正确性见 ocean-draw 头测(pin trail/lasso/scope 淡化)；npm build 通过；浏览器交互+性能 M2.5"
      }
    },
    {
      "notes": "",
      "path": "web/src/views/Rotation.test.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Rotation SSR 渲染 + rotation-draw 纯函数测试（AC-M3/AC-M4）：renderToStaticMarkup 喂提交版 rotation.sample.json(sector) + rotation.theme.sample.json(theme)",
        "invariants": "用 export 真实产物 fixture(非手写)；sector：总览 ≥11 path/4 态/league/y=100/params、钻进 N=1+成员跳转、rotation-draw geometry(夹 100/null 断线/斜率分段/标签防重叠)；theme(M4.4)：总览 path 数=count(4)+不含「待 M4」+THEME_VAR(--th-*)色+「非市值加权」标注、theme drill(scope.kind=theme)N=1+成员；SVG SSR 可断言(非 canvas、effect 不跑)",
        "verification": "npx vitest run：Rotation.test 14 项过(含 4 theme)、不破坏 Ocean(23)/Discovery(10)/composite(4) 既有；npm run build tsc 通过"
      }
    },
    {
      "notes": "",
      "path": "web/src/views/Rotation.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Rotation surface（PRD §9.4）：bucket RS-Ratio SVG 多线总览 + enriched league 表 + 钻进 N=1 单线(斜率色)+summary+成员预览 + 常驻周日期轴 + hover 时间游标；GICS↔Theme 切换两种 bucket 模式均真实渲染(M4.4)",
        "invariants": "读 rotation.json(sector)/rotation.theme.json(theme，toggle 首次切 theme 时 lazy-load)；initial 注入免 fetch(SSR/test)、其 bucket_type 定起始视图；theme 模式复用同一多线/league/钻进组件、经 bucketColorVar 上 THEME_VAR(--th-*)色；总览 hover 改由 svg 级 mousemove 驱动(按 viewBox-y 选最近线高亮 + 周游标)、点击 pick 当前高亮线、league 行/end label 仍可 hover/pick(共享 hover state)；右缘末值排序标签、y=100 基线；点 league 行/线→setScope({kind:bucketType}) 第 2 个 scope writer(C10)+原地 inline drawer 不 auto-jump(§9.1.2)、drill 当 scope.kind===当前 bucketType；N=1 线色=短窗斜率(↑绿↓红)=momentum、drill 保留原始周 index 对齐(soloScale/segments 仍见紧凑无 gap 序列)；两图底部常驻 MM/DD 周刻度；时间游标=竖线+最近线(总览)/该线(drill)RS marker + bucket·date·RS 读数，为 hover-only overlay 故 SSR 不渲染游标、不改 path 数=count 断言；成员预览复用 Discovery(已按 theme scope 过滤 s.themes — C9/DRY)不重复造；theme 总览标注 point-in-time + 非市值加权；params 暴露透明 reconstruction 不复刻 StockCharts",
        "verification": "Rotation.test.tsx SSR：sector 总览 path 数=count(≥11)+4 态+league+y=100+params、钻进 N=1+summary+「看全部成员」；theme 总览 path 数=count(4)+不含「待 M4」占位+THEME_VAR(--th-*)色+「非市值加权」标注、theme drill(scope.kind=theme)N=1+成员；游标 hover-only 不破 path 数断言；npx vitest run(14 项)+ npm run build(tsc)通过"
      }
    },
    {
      "notes": "",
      "path": "web/src/views/Stock.test.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Stock 视图 + StockStack AC（PRD §9.6，M5.4）+ 点火诊断(M7.4 PRD §10.8)的 SSR 回归门",
        "invariants": "用 stock.sample.json(从真实 bundle 再生、含 ignition 块)+renderToStaticMarkup 无 fetch；断言 Stock 头部/theme chip exposure%/6 估值卡/composite 5 分量/四格 stack；StockStack 四格标签/季度网格/PS 线/空 price 安全；M7.4 点火诊断断言：渲染 5 ignition 分量 + 点火证据 strip + persistence timeline；composite 不被 ignition 替换(5 分量仍在)；🔥持续点火 candidate(注入 TT22 真实块)亮 persist 日格、○未点火(fixture TT07 persist=0)无 lit 格；step_rate clamp(>20× 显 &gt;20×)；旋钮 k(0↔1)不改 ignition 块；无 ignition(null)整块隐藏(back-compat)",
        "verification": "npm test 全过；与 Stock.tsx + StockStack 合同一致"
      }
    },
    {
      "notes": "",
      "path": "web/src/views/Stock.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Stock detail 视图（PRD §9.6，M5.4 正式版 + M7.4 点火诊断）：per-name bundle 懒加载 + 四格时间轴对齐 stack + 并列第二引擎 ignition 诊断小节",
        "invariants": "per-name、不受 scope；懒加载 stock/<ticker>.json(loadStockBundle，ticker 变重取)+stock/index.json(下拉)或 initial 注入(SSR/测试)；核心 StockStack 四格(替代 #53 单 MiniChart)；头部 ticker+sector·mktcap+theme chips(exposure%)+COMPOSITE(composite(components,k) 重算)；6 估值卡(bundle.valuation)+composite 5 分量(权重随 k)；M7.4 在 stack 与估值卡之间插「点火诊断」小节(bundle.ignition 存在才渲染、缺则整块隐藏，back-compat)：persistence timeline(PersistTimeline 离散日格、lit 格=连续 top-decile 日、capped 10) + 🔥持续点火/○未点火 + ign_pct/persist/score 读数 + 点火证据 strip(复用 .ec-ignev，brk/vol/step/MA50，step 经 fmtStepRate clamp) + ignition 5 原始分量(accel/expand/vsurge/breakout/rsturn，原始值非 [0,1] 故不画百分比条)；ignition 块 verbatim 自 bundle(同源 C9、勿重算)、early⟷reliable 旋钮 k 只重算 composite 不动 ignition(PRD P7)；composite 5 分量 stack 保留不删(点火诊断是新增并列小节)；读同一 valuation_daily/fundamentals_q(C9)；filing AI 摘要留 M5 之后",
        "verification": "Stock.test SSR(注入 bundle)断言头部/theme chip exposure%/6 卡/composite 5 分量/四格 stack + 点火诊断(5 ignition 分量/点火证据/persistence timeline/🔥candidate/○未点火/step clamp/旋钮不动 ignition/无 ignition 隐藏)；浏览器 smoke 懒加载 + 四格渲染"
      }
    },
    {
      "notes": "",
      "path": "web/src/views/Valuation.test.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Valuation 视图 AC（PRD §9.5，M5.2）的 SSR 回归门 + buildValuationSql 单测",
        "invariants": "用 valuation.sample.json（从 parquet 转）+ renderToStaticMarkup 无 duckdb-wasm；断言全 universe 行 / PS 升序便宜在上(nulls last) / PEG+Mgn% 列 / 三档色+vint / sector+theme(themes 列)scope 过滤；buildValuationSql 测 ORDER BY metric ASC/DESC + NULLS LAST",
        "verification": "npm test 全过；与 Valuation.tsx + lib/duckdb 合同一致"
      }
    },
    {
      "notes": "",
      "path": "web/src/views/Valuation.tsx",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Valuation screener 视图（PRD §9.5，M5.2 正式版）：duckdb-wasm 浏览器查 valuation.parquet 渲染全 universe 横截面表",
        "invariants": "数据从 duckdb-wasm 查 valuation.parquet（lib/duckdb.queryValuation，metric 变重查）或 initial 注入（SSR/测试，JS sortRows 同序）；列含 PEG/Mgn%（board.json 预览缺的两列）；as-of 三档上色取 freshness（fresh 绿/stale 黄/overdue 红变暗 C7/§10.5）；pctile 仅 fresh cohort 内排（common-vintage §10.5）、非 fresh 显 vint；scope 下拉=全局 writer，先 filter（sector/theme[themes 列 split]/pinned）再排序再 pctile；读同一 valuation_daily（与 board/Ocean C9 同源）",
        "verification": "Valuation.test SSR（注入 rows）断言全 universe 行 / PS 升序便宜在上 / PEG+Mgn% 列 / 三档色+vint / sector+theme scope 过滤；浏览器 smoke duckdb-wasm 实查"
      }
    },
    {
      "notes": "",
      "path": "web/src/vite-env.d.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Vite 客户端环境类型声明（import.meta.env 等）",
        "invariants": "仅一行 /// <reference types=\"vite/client\" />；为 tsc 提供 ImportMetaEnv(BASE_URL 等)类型；无运行时产物",
        "verification": "tsc 识别 import.meta.env.BASE_URL 无类型错；npm run build 的 tsc 段通过"
      }
    },
    {
      "notes": "",
      "path": "web/tsconfig.json",
      "kind": "config",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "TypeScript 编译配置（bundler 模式、strict、noEmit 仅类型检查）",
        "invariants": "strict 全开 + noUnusedLocals/Parameters；jsx=react-jsx；moduleResolution=bundler；noEmit（打包交给 Vite）；include 仅 src",
        "verification": "tsc 通过即类型契约成立；npm run build 的 tsc 段无错"
      }
    },
    {
      "notes": "",
      "path": "web/vite.config.ts",
      "kind": "code",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Vite 构建 + Vitest 测试配置：React 插件 + base 相对路径 + test(node env, src/**/*.test.{ts,tsx})",
        "invariants": "base='./' 子路径可用；输出 web/dist(gitignore)；运行时数据从 public/data 加载、构建不内联；test 段跑 C9 parity + AC-M1 渲染测试(.ts/.tsx)",
        "verification": "vite build 成功且资源相对路径；make web-test 跑 composite.test.ts + Discovery.test.tsx"
      }
    },
    {
      "notes": "",
      "path": "web/wrangler.jsonc",
      "kind": "asset",
      "module": "web",
      "stage": "implemented",
      "contract": {
        "purpose": "Cloudflare Pages 配置(D.3)：声明项目名 + 构建产物目录，供 `wrangler pages deploy` 直传 web/dist 生产公开上线",
        "invariants": "纯静态站点(无 Functions/bindings/vars)；绝不含 secret(token 走 wrangler login OAuth 或 CI secret)；name=tickertide(改名同步 Deploy-Pages-Runbook.md)；pages_build_output_dir=dist(相对 web/，即 web/dist)；生产访问公开，预览 Access 保护由 Cloudflare Zero Trust 带外配置、不在此声明",
        "verification": "`cd web && npx wrangler pages deploy --branch=main` 读本文件 name+output dir 直传成功；grep 确认无 token/account 值入库"
      }
    }
  ],
  "retired_entries": [],
  "last_structure_digest": "e0a9583bdd5d6a5dde9fd32b85baf4550337a5a4e3ff1d8eeea269c300be200a"
}
