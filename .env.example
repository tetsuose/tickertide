# TickerTide secrets — TEMPLATE (names only). Copy to .env (gitignored) and fill.
# See docs/runtime/Credentials-Management.md. Never commit real values.

# Stooq per-symbol CSV now requires an apikey (get via captcha at stooq.com).
# Only needed if you run ingest with --provider stooq; the M0 default is yfinance.
STOOQ_APIKEY=
